name: common_errors_from_bam
description: common_errors_from_bam
usage: common_errors_from_bam [-h] [-o OUTPUT_PREFIX] bam ref_fasta
options:
  - names:
      - -h
      - --help
    argument: ""
    description: show this help message and exit
  - names:
      - -o
      - --output_prefix
    argument: OUTPUT_PREFIX
    description: 'Prefix for outputs. (default: common_errors)'
version: common_errors_from_bam (pomoxis 0.3.10)