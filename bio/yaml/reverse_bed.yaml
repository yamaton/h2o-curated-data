name: reverse_bed
usage: reverse_bed [-h] bed_in ref_fasta bed_out
description: Convert bed-file coordinates to coordinates on the reverse strand.
positionalArguments:
  - name: bed_in
    description: Input bed file.
  - name: ref_fasta
    description: Input reference fasta file.
  - name: bed_out
    description: Output bed file.
options:
  - names:
      - -h
      - --help
    argument: ""
    description: show this help message and exit
