name: tag_bam
usage: tag_bam [-h] input output tag_name tag_value
description: Add a tag to all alignments in a bam.
positionalArguments:
  - name: input
    description: Input bam file.
  - name: output
    description: Output output file.
  - name: tag_name
    description: Tag name.
  - name: tag_value
    description: Tag value.
options:
  - names:
      - -h
      - --help
    argument: ""
    description: show this help message and exit
version: tag_bam (pomoxis 0.3.10)
