name: vcfcombine
description: vcfcombine
options:
  - names:
      - -h
      - --help
    argument: ""
    description: This text.
  - names:
      - -v
      - --version
    argument: ""
    description: Print version.
  - names:
      - -r
      - --region
    argument: REGION
    description: A region specifier of the form chrN:x-y to bound the merge
