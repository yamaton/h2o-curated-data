name: vcfleftalign
description: vcfleftalign
options:
  - names:
      - -r
      - --reference
    argument: FILE
    description: Use this reference as a basis for realignment.
  - names:
      - -w
      - --window
    argument: "N"
    description: Use a window of this many bp when left aligning (150).
