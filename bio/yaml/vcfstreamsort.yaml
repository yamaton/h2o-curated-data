name: vcfstreamsort
description: vcfstreamsort
options:
  - names:
      - -h
      - --help
    argument: ""
    description: this dialog
  - names:
      - -w
      - --window
    argument: "N"
    description: number of sites to sort (default 10000)
  - names:
      - -a
      - --all
    argument: ""
    description: load all sites and then sort in memory
