#compdef _bioawk bioawk

# Generated with h2o 0.1.18


function _bioawk {
    local line state

    function _commands {
        local -a commands
        commands=(
        )
        _describe 'command' commands
    }
 

    _arguments -C \
        '-t[Equivalent to -F'\''\t'\'' -v OFS="\t"]' \
        '-c[Specify input format (hdr|header|sam|vcf|bed|gff|fastx)]' \
        '-f[Program file as input.]:file:_files' \
        '-v[Assign value to variable `var` before program is executed]' \
        '-F[Define input field separator as regular expression]' \
        '1: :_commands' \
        '*: :_files'

    case $state in
    (cmd)
        _commands
        ;;
    (subcmd)
        case $line[1] in
        esac
        ;;
     esac

}

