#compdef _makehmmerdb makehmmerdb

# Generated with h2o 0.1.18


function _makehmmerdb {
    local line state

    function _commands {
        local -a commands
        commands=(
        )
        _describe 'command' commands
    }
 

    _arguments -C \
        '-h[show brief help on version and usage]' \
        '--informat[specify that input file is in format <s>]' \
        '--bin_length[bin length (power of 2;  32<=b<=4096)  \[256\]]' \
        '--sa_freq[suffix array sample rate (power of 2)  \[8\]]' \
        '--block_size[input sequence broken into blocks this size (Mbases)  \[50\]]' \
        '1: :_commands' \
        '*: :_files'

    case $state in
    (cmd)
        _commands
        ;;
    (subcmd)
        case $line[1] in
        esac
        ;;
     esac

}

