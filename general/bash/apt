# Generated with h2o 0.1.18

# autogenerated bash script

_apt()
{
    local i=1 cmd cur word_list
    cur="${COMP_WORDS[COMP_CWORD]}"

    # take the last word that's NOT starting with -
    while [[ ( "$i" < "$COMP_CWORD" ) ]]; do
        local s="${COMP_WORDS[i]}"
        case "$s" in
          -*) ;;
          *)
            cmd="$s"
            ;;
        esac
        (( i++ ))
    done

    case "$cmd" in
          list) _apt_list
            return
            ;;

          search) _apt_search
            return
            ;;

          show) _apt_show
            return
            ;;

          install) _apt_install
            return
            ;;

          reinstall) _apt_reinstall
            return
            ;;

          remove) _apt_remove
            return
            ;;

          autoremove) _apt_autoremove
            return
            ;;

          update) _apt_update
            return
            ;;

          upgrade) _apt_upgrade
            return
            ;;

          full-upgrade) _apt_full-upgrade
            return
            ;;

          edit-sources) _apt_edit-sources
            return
            ;;

          satisfy) _apt_satisfy
            return
            ;;

      *)
          word_list=" list search show install reinstall remove autoremove update upgrade full-upgrade edit-sources satisfy -h --help -v --version -c --config-file -o --option" 
          COMPREPLY=( $(compgen -W "${word_list}" -- "${cur}") )
          ;;
    esac

}

_apt_list ()
{
    local cur word_list
    word_list=" --installed --upgradable --all-versions" 
    cur="${COMP_WORDS[COMP_CWORD]}"
    COMPREPLY=( $(compgen -W "${word_list}" -- "${cur}") )
}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _apt apt
