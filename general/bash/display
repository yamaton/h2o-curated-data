# Generated with h2o 0.1.18

# autogenerated bash script

_display()
{
    local i=1 cmd cur word_list
    cur="${COMP_WORDS[COMP_CWORD]}"

    # take the last word that's NOT starting with -
    while [[ ( "$i" < "$COMP_CWORD" ) ]]; do
        local s="${COMP_WORDS[i]}"
        case "$s" in
          -*) ;;
          *)
            cmd="$s"
            ;;
        esac
        (( i++ ))
    done

    case "$cmd" in
      *)
          word_list="  -alpha -antialias -authenticate -backdrop -channel -colormap -colorspace -comment -compress -define -delay -density -depth -display -dispose -dither -endian -filter -format -geometry -gravity -identify -immutable -interlace -interpolate -label -limit -loop -map -matte -monitor -nostdin -page -profile -quality -quantize -quiet -regard-warnings -remote -repage -sampling-factor -scenes -seed -set -size -support -texture -transparent-color -treedepth -update -verbose -visual -virtual-pixel -window -window-group -write -auto-orient -border -clip -clip-path -colors -contrast -crop -decipher -deskew -despeckle -edge -enhance -equalize -extract -flip -flop -frame -fuzz -gamma -monochrome -negate -normalize -raise -resample -resize -roll -rotate -sample -segment -sharpen -strip -threshold -thumbnail -trim -coalesce -flatten -debug -help -list -log -version" 
          COMPREPLY=( $(compgen -W "${word_list}" -- "${cur}") )
          ;;
    esac

}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _display display
