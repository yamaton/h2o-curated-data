# Generated with h2o 0.1.18

# autogenerated bash script

_import()
{
    local i=1 cmd cur word_list
    cur="${COMP_WORDS[COMP_CWORD]}"

    # take the last word that's NOT starting with -
    while [[ ( "$i" < "$COMP_CWORD" ) ]]; do
        local s="${COMP_WORDS[i]}"
        case "$s" in
          -*) ;;
          *)
            cmd="$s"
            ;;
        esac
        (( i++ ))
    done

    case "$cmd" in
      *)
          word_list="  -adjoin -border -channel -colorspace -comment -compress -define -density -depth -descend -display -dispose -dither -delay -encipher -endian -encoding -filter -format -frame -gravity -identify -interlace -interpolate -label -limit -monitor -page -pause -pointsize -quality -quiet -regard-warnings -repage -sampling-factor -scene -screen -seed -set -silent -snaps -support -synchronize -taint -transparent-color -treedepth -verbose -virtual-pixel -window -annotate -colors -crop -geometry -help -monochrome -negate -quantize -resize -rotate -strip -thumbnail -transparent -trim -type -debug -list -log -version" 
          COMPREPLY=( $(compgen -W "${word_list}" -- "${cur}") )
          ;;
    esac

}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _import import
