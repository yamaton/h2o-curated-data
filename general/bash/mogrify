# Auto-generated with h2o

_mogrify()
{
    local i=1 cmd cur word_list
    cur="${COMP_WORDS[COMP_CWORD]}"

    # take the last word that's NOT starting with -
    while [[ ( "$i" < "$COMP_CWORD" ) ]]; do
        local s="${COMP_WORDS[i]}"
        case "$s" in
          -*) ;;
          *)
            cmd="$s"
            ;;
        esac
        (( i++ ))
    done

    case "$cmd" in
      *)
          word_list="  -adjoin -affine -alpha -antialias -authenticate -attenuate -background -bias -black-point-compensation -blue-primary -bordercolor -caption -colorspace -comment -compose -compress -define -delay -density -depth -direction -display -dispose -dither -encoding -endian -family -features -fill -filter -font -format -gravity -green-primary -illuminant -intensity -intent -interlace -interline-spacing -interpolate -interword-spacing -kerning -label -limit -loop -matte -mattecolor -monitor -orient -page -path -ping -pointsize -precision -preview -quality -quiet -read-mask -red-primary -regard-warnings -remap -respect-parentheses -sampling-factor -scene -seed -size -stretch -stroke -strokewidth -style -synchronize -taint -texture -tile-offset -treedepth -transparent-color -undercolor -units -verbose -view -virtual-pixel -weight -white-point -word-break -adaptive-blur -adaptive-resize -adaptive-sharpen -alpha -annotate -auto-gamma -auto-level -auto-orient -auto-threshold -bench -bilateral-blur -black-threshold -blue-shift -blur -border -brightness-contrast -canny -cdl -channel -charcoal -chop -clahe -clamp -clip -clip-mask -clip-path -colorize -color-matrix -colors -color-threshold -connected-components -contrast -contrast-stretch -convolve -cycle -decipher -deskew -despeckle -distort -draw -edge -encipher -emboss -enhance -equalize -evaluate -extent -extract -fft -flip -floodfill -flop -frame -function -gamma -gaussian-blur -geometry -grayscale -hough-lines -identify -ift -implode -integral -interpolative-resize -kmeans -kuwahara -lat -level -level-colors -linear-stretch -liquid-rescale -local-contrast -magnify -mean-shift -median -mode -modulate -monochrome -morphology -motion-blur -negate -noise -normalize -opaque -ordered-dither -paint -perceptible -polaroid -posterize -profile -quantize -raise -random-threshold -range-threshold -region -render -repage -resample -resize -roll -rotate -rotational-blur -sample -scale -segment -selective-blur -sepia-tone -set -shade -shadow -sharpen -shave -shear -sigmoidal-contrast -sketch -solarize -sort-pixels -sparse-color -splice -spread -statistic -strip -swirl -threshold -thumbnail -tile -tint -transform -transparent -transpose -transverse -trim -type -unique-colors -unsharp -vignette -wave -wavelet-denoise -white-balance -white-threshold -channel-fx -separate -affinity -append -clut -coalesce -combine -compare -complex -composite -copy -crop -deconstruct -evaluate-sequence -flatten -fx -hald-clut -layers -morph -mosaic -poly -print -process -smush -write -delete -duplicate -insert -reverse -swap -debug -distribute-cache -help -list -log -version"
          COMPREPLY=( $(compgen -W "${word_list}" -- "${cur}") )
          ;;
    esac

}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _mogrify mogrify
