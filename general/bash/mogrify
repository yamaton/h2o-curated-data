# Auto-generated with h2o

_mogrify()
{
    local i=1 cmd cur word_list
    cur="${COMP_WORDS[COMP_CWORD]}"

    # take the last word that's NOT starting with -
    while [[ ( "$i" < "$COMP_CWORD" ) ]]; do
        local s="${COMP_WORDS[i]}"
        case "$s" in
          -*) ;;
          *)
            cmd="$s"
            ;;
        esac
        (( i++ ))
    done

    case "$cmd" in
      *)
          word_list="  -adjoin -affine -alpha -antialias -authenticate -attenuate -background -bias -black-point-compensation -blue-primary -bordercolor -caption -cdl -channel -colors -colorspace -comment -compose -compress -decipher -define -delay -density -depth -direction -display -dispose -dither -encipher -encoding -endian -family -features -fill -filter -flatten -font -format -function -fuzz -gravity -green-primary -intensity -intent -interlace -interline-spacing -interpolate -interword-spacing -kerning -label -limit -loop -mask -matte -mattecolor -monitor -morphology -orient -page -path -ping -pointsize -precision -preview -quality -quiet -red-primary -regard-warnings -remap -repage -sampling-factor -scene -seed -size -stretch -stroke -strokewidth -style -synchronize -taint -texture -tile-offset -treedepth -transparent-color -undercolor -units -verbose -view -virtual-pixel -weight -white-point -adaptive-blur -adaptive-resize -adaptive-sharpen -alpha -annotate -auto-gamma -auto-level -auto-orient -bench -black-threshold -blue-shift -blur -border -brightness-contrast -canny -charcoal -chop -clamp -clip -clip-mask -clip-path -colorize -connected-components -contrast -contrast-stretch -convolve -cycle -deskew -despeckle -distort -draw -edge -emboss -enhance -equalize -evaluate -extent -extract -hough-lines -fft -flip -floodfill -flop -frame -function -gamma -gaussian-blur -geometry -grayscale -help -identify -ift -implode -kuwahara -lat -layers -level -level-colors -linear-stretch -liquid-rescale -local-contrast -magnify -mean-shift -median -mode -modulate -monochrome -motion-blur -negate -noise -normalize -opaque -ordered-dither -paint -perceptible -polaroid -posterize -profile -quantize -radial-blur -raise -random-threshold -region -render -resample -resize -roll -rotate -sample -scale -segment -selective-blur -sepia-tone -set -shade -shadow -sharpen -shave -shear -sigmoidal-contrast -sketch -solarize -sparse-color -splice -spread -statistic -strip -swirl -threshold -thumbnail -tile -tint -transform -transparent -transpose -transverse -trim -type -unique-colors -unsharp -vignette -wave -wavelet-denoise -white-threshold -affinity -append -clut -coalesce -combine -compare -complex -composite -copy -crop -deconstruct -evaluate-sequence -fx -hald-clut -layers -morph -mosaic -poly -print -process -separate -smush -write -delete -duplicate -insert -reverse -swap -debug -distribute-cache -list -log -version" 
          COMPREPLY=( $(compgen -W "${word_list}" -- "${cur}") )
          ;;
    esac

}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _mogrify mogrify
