# Auto-generated with h2o

_montage()
{
    local i=1 cmd cur word_list
    cur="${COMP_WORDS[COMP_CWORD]}"

    # take the last word that's NOT starting with -
    while [[ ( "$i" < "$COMP_CWORD" ) ]]; do
        local s="${COMP_WORDS[i]}"
        case "$s" in
          -*) ;;
          *)
            cmd="$s"
            ;;
        esac
        (( i++ ))
    done

    case "$cmd" in
      *)
          word_list="  -adjoin -affine -alpha -authenticate -blue-primary -bordercolor -caption -channel -colors -colorspace -comment -compose -compress -define -density -depth -display -dispose -dither -draw -encoding -endian -extract -fill -filter -font -format -gamma -geometry -gravity -green-primary -identify -interlace -interpolate -kerning -label -limit -matte -mattecolor -mode -monitor -origin -page -pointsize -profile -quality -quantize -quiet -red-primary -regard-warnings -repage -sampling-factor -scenes -seed -set -shadow -size -stroke -support -synchronize -taint -texture -thumbnail -tile -title -transparent-color -treedepth -trim -units -verbose -virtual-pixel -white-point -adaptive-sharpen -annotate -auto-orient -blur -border -crop -extent -flatten -flip -flop -frame -monochrome -polaroid -resize -rotate -scale -strip -transform -transpose -transparent -type -unsharp -coalesce -composite -clone -delete -duplicate -insert -reverse -swap -debug -help -list -log -version" 
          COMPREPLY=( $(compgen -W "${word_list}" -- "${cur}") )
          ;;
    esac

}

## -o bashdefault and -o default are fallback
complete -o bashdefault -o default -F _montage montage
