# Auto-generated with h2o

complete -c endlessh -s "4" -d "Forces endless to use IPv4 addresses only."
complete -c endlessh -s "6" -d "Forces endless to use IPv6 addresses only."
complete -c endlessh -s "d" -d "Message milliseconds delay." -x
complete -c endlessh -s "f" -d "Set and load config file." -r
complete -c endlessh -s "h" -d "Print the help message and exit."
complete -c endlessh -s "l" -d "Maximum banner line length (3-255)." -x
complete -c endlessh -s "m" -d "Maximum number of clients." -x
complete -c endlessh -s "p" -d "Set the listening port." -x
complete -c endlessh -s "s" -d "Print diagnostics to syslog."
complete -c endlessh -s "v" -d "Print diagnostics."
complete -c endlessh -s "V" -d "Causes endless to print version information and exit."
