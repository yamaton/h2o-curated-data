# Auto-generated with h2o

complete -c file -l "help" -d "display this help and exit"
complete -c file -s "v" -l "version" -d "output version information and exit"
complete -c file -s "m" -l "magic-file" -d "use LIST as a colon-separated list of magic" -x
complete -c file -s "z" -l "uncompress" -d "try to look inside compressed files"
complete -c file -s "Z" -l "uncompress-noreport" -d "only print the contents of compressed files"
complete -c file -s "b" -l "brief" -d "do not prepend filenames to output lines"
complete -c file -s "c" -l "checking-printout" -d "print the parsed form of the magic file, use in"
complete -c file -s "e" -l "exclude" -d "exclude TEST from the list of test to be" -x
complete -c file -l "exclude-quiet" -d "like exclude, but ignore unknown tests,-f, --files-from FILE   read the filenames to be examined from FILE" -r
complete -c file -s "F" -l "separator" -d "use string as separator instead of `:'" -x
complete -c file -s "i" -l "mime" -d "output MIME type strings (--mime-type and"
complete -c file -l "apple" -d "output the Apple CREATOR/TYPE"
complete -c file -l "extension" -d "output a slash-separated list of extensions"
complete -c file -l "mime-type" -d "output the MIME type"
complete -c file -l "mime-encoding" -d "output the MIME encoding"
complete -c file -s "k" -l "keep-going" -d "don't stop at the first match"
complete -c file -s "l" -l "list" -d "list magic strength"
complete -c file -s "L" -l "dereference" -d "follow symlinks (default if POSIXLY_CORRECT is set)"
complete -c file -s "h" -l "no-dereference" -d "don't follow symlinks (default if POSIXLY_CORRECT is not set) (default)"
complete -c file -s "n" -l "no-buffer" -d "do not buffer output"
complete -c file -s "N" -l "no-pad" -d "do not pad output"
complete -c file -s "0" -l "print0" -d "terminate filenames with ASCII NUL"
complete -c file -s "p" -l "preserve-date" -d "preserve access times on files"
complete -c file -s "P" -l "parameter" -d "set file engine parameter limits"
complete -c file -s "r" -l "raw" -d "don't translate unprintable chars to \\ooo"
complete -c file -s "s" -l "special-files" -d "treat special (block/char devices) files as ordinary ones"
complete -c file -s "S" -l "no-sandbox" -d "disable system call sandboxing"
complete -c file -s "C" -l "compile" -d "compile file specified by -m"
complete -c file -s "d" -l "debug" -d "print debugging messages"
