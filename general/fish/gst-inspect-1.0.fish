# Auto-generated with h2o

complete -c gst-inspect-1.0 -s "h" -l "help" -d "Show help options"
complete -c gst-inspect-1.0 -l "help-all" -d "Show all help options"
complete -c gst-inspect-1.0 -l "help-gst" -d "Show GStreamer Options"
complete -c gst-inspect-1.0 -s "a" -l "print-all" -d "Print all elements"
complete -c gst-inspect-1.0 -s "b" -l "print-blacklist" -d "Print list of blacklisted files"
complete -c gst-inspect-1.0 -l "print-plugin-auto-install-info" -d "Print a machine-parsable list of features the specified plugin or all plugins provide."
complete -c gst-inspect-1.0 -l "plugin" -d "List the plugin contents"
complete -c gst-inspect-1.0 -s "t" -l "types" -d "A slashes ('/') separated list of types of elements (also known as klass) to list."
complete -c gst-inspect-1.0 -l "exists" -d "Check if the specified element or plugin exists"
complete -c gst-inspect-1.0 -l "atleast-version" -d "When checking if an element or plugin exists, also check that its version is at least the version specified"
complete -c gst-inspect-1.0 -s "u" -l "uri-handlers" -d "Print supported URI schemes, with the elements that implement them"
complete -c gst-inspect-1.0 -l "no-colors" -d "Disable colors in output."
complete -c gst-inspect-1.0 -l "sort" -d "Sort plugins and features." -x
complete -c gst-inspect-1.0 -s "C" -l "color" -d "Color output, even when not sending to a tty."
complete -c gst-inspect-1.0 -l "version" -d "Print version information and exit"
