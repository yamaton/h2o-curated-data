# Auto-generated with h2o

complete -c gst-launch-1.0 -s "h" -l "help" -d "Show help options"
complete -c gst-launch-1.0 -l "help-all" -d "Show all help options"
complete -c gst-launch-1.0 -l "help-gst" -d "Show GStreamer Options"
complete -c gst-launch-1.0 -s "t" -l "tags" -d "Output tags (also known as metadata)"
complete -c gst-launch-1.0 -s "c" -l "toc" -d "Output TOC (chapters and editions)"
complete -c gst-launch-1.0 -s "v" -l "verbose" -d "Output status information and property notifications"
complete -c gst-launch-1.0 -s "q" -l "quiet" -d "Do not print any progress information"
complete -c gst-launch-1.0 -s "m" -l "messages" -d "Output messages"
complete -c gst-launch-1.0 -s "X" -l "exclude" -d "Do not output status information for the specified property if verbose output is enabled (can be used multiple times)" -x
complete -c gst-launch-1.0 -s "f" -l "no-fault" -d "Do not install a fault handler"
complete -c gst-launch-1.0 -s "e" -l "eos-on-shutdown" -d "Force EOS on sources before shutting the pipeline down"
complete -c gst-launch-1.0 -l "version" -d "Print version information and exit"
complete -c gst-launch-1.0 -l "no-position" -d "Do not print current position of pipeline."
complete -c gst-launch-1.0 -l "force-position" -d "Allow printing current position of pipeline even if stdout is not a TTY."
