# Auto-generated with h2o

complete -c ldconfig -s "c" -l "format" -d "Format to use: new (default), old, or compat" -x
complete -c ldconfig -s "C" -d "Use CACHE as cache file" -r
complete -c ldconfig -s "f" -d "Use CONF as configuration file" -r
complete -c ldconfig -s "i" -l "ignore-aux-cache" -d "Ignore auxiliary cache file"
complete -c ldconfig -s "l" -d "Manually link individual libraries."
complete -c ldconfig -s "n" -d "Only process directories specified on the command line."
complete -c ldconfig -s "N" -d "Don't build cache"
complete -c ldconfig -s "p" -l "print-cache" -d "Print cache"
complete -c ldconfig -s "r" -d "Change to and use ROOT as root directory" -r
complete -c ldconfig -s "v" -l "verbose" -d "Generate verbose messages"
complete -c ldconfig -s "X" -d "Don't update symbolic links"
complete -c ldconfig -s "?" -l "help" -d "Give this help list"
complete -c ldconfig -l "usage" -d "Give a short usage message"
complete -c ldconfig -s "V" -l "version" -d "Print program version"
