# Auto-generated with h2o

complete -c ls -s "a" -l "all" -d "do not ignore entries starting with ."
complete -c ls -s "A" -l "almost-all" -d "do not list implied ."
complete -c ls -l "author" -d "with -l, print the author of each file"
complete -c ls -s "b" -l "escape" -d "print C-style escapes for nongraphic characters"
complete -c ls -l "block-size" -d "with -l, scale sizes by SIZE when printing them; e.g., '--block-size=M'; see SIZE format below" -x
complete -c ls -s "B" -l "ignore-backups" -d "do not list implied entries ending with ~"
complete -c ls -s "c" -d "with -lt: sort by, and show, ctime (time of last modification of file status information); with -l: show ctime and sort by name; otherwise: sort by ctime, newest first"
complete -c ls -s "C" -d "list entries by columns"
complete -c ls -l "color" -d "colorize the output; WHEN can be 'always' (default if omitted), 'auto', or 'never'; more info below" -x
complete -c ls -s "d" -l "directory" -d "list directories themselves, not their contents"
complete -c ls -s "D" -l "dired" -d "generate output designed for Emacs' dired mode"
complete -c ls -s "f" -d "do not sort, enable -aU, disable -ls --color"
complete -c ls -s "F" -l "classify" -d "append indicator (one of */=>@|) to entries"
complete -c ls -l "file-type" -d "likewise, except do not append '*'"
complete -c ls -l "format" -d "across -x, commas -m, horizontal -x, long -l, single-column -1, verbose -l, vertical -C" -x
complete -c ls -l "full-time" -d "like -l --time-style=full-iso"
complete -c ls -s "g" -d "like -l, but do not list owner"
complete -c ls -l "group-directories-first" -d "group directories before files;"
complete -c ls -s "G" -l "no-group" -d "in a long listing, don't print group names"
complete -c ls -s "h" -l "human-readable" -d "with -l and -s, print sizes like 1K 234M 2G etc."
complete -c ls -l "si" -d "likewise, but use powers of 1000 not 1024"
complete -c ls -s "H" -l "dereference-command-line" -d "follow symbolic links listed on the command line"
complete -c ls -l "dereference-command-line-symlink-to-dir" -d "follow each command line symbolic link"
complete -c ls -l "hide" -d "do not list implied entries matching shell PATTERN (overridden by -a or -A)" -x
complete -c ls -l "hyperlink" -d "hyperlink file names; WHEN can be 'always' (default if omitted), 'auto', or 'never'" -r
complete -c ls -l "indicator-style" -d "append indicator with style WORD to entry names: none (default), slash (-p), file-type (--file-type), classify (-F)" -r
complete -c ls -s "i" -l "inode" -d "print the index number of each file"
complete -c ls -s "I" -l "ignore" -d "do not list implied entries matching shell PATTERN" -x
complete -c ls -s "k" -l "kibibytes" -d "default to 1024-byte blocks for disk usage; used only with -s and per directory totals"
complete -c ls -s "l" -d "use a long listing format"
complete -c ls -s "L" -l "dereference" -d "when showing file information for a symbolic link, show information for the file the link references rather than for the link itself"
complete -c ls -s "m" -d "fill width with a comma separated list of entries"
complete -c ls -s "n" -l "numeric-uid-gid" -d "like -l, but list numeric user and group IDs"
complete -c ls -s "N" -l "literal" -d "print entry names without quoting"
complete -c ls -s "o" -d "like -l, but do not list group information"
complete -c ls -s "p" -l "indicator-style" -d "append / indicator to directories" -r
complete -c ls -s "q" -l "hide-control-chars" -d "print ? instead of nongraphic characters"
complete -c ls -l "show-control-chars" -d "show nongraphic characters as-is (the default, unless program is 'ls' and output is a terminal)"
complete -c ls -s "Q" -l "quote-name" -d "enclose entry names in double quotes"
complete -c ls -l "quoting-style" -d "use quoting style WORD for entry names: literal, locale, shell, shell-always, shell-escape, shell-escape-always, c, escape (overrides QUOTING_STYLE environment variable)" -x
complete -c ls -s "r" -l "reverse" -d "reverse order while sorting"
complete -c ls -s "R" -l "recursive" -d "list subdirectories recursively"
complete -c ls -s "s" -l "size" -d "print the allocated size of each file, in blocks"
complete -c ls -s "S" -d "sort by file size, largest first"
complete -c ls -l "sort" -d "sort by WORD instead of name: none (-U), size (-S), time (-t), version (-v), extension (-X)" -x
complete -c ls -l "time" -d "change the default of using modification times; access time (-u): atime, access, use; change time (-c): ctime, status; birth time: birth, creation;" -x
complete -c ls -l "time-style" -d "time/date format with -l; see TIME_STYLE below" -x
complete -c ls -s "t" -d "sort by time, newest first; see --time"
complete -c ls -s "T" -l "tabsize" -d "assume tab stops at each COLS instead of 8" -x
complete -c ls -s "u" -d "with -lt: sort by, and show, access time; with -l: show access time and sort by name; otherwise: sort by access time, newest first"
complete -c ls -s "U" -d "do not sort; list entries in directory order"
complete -c ls -s "v" -d "natural sort of (version) numbers within text"
complete -c ls -s "w" -l "width" -d "set output width to COLS." -x
complete -c ls -s "x" -d "list entries by lines instead of by columns"
complete -c ls -s "X" -d "sort alphabetically by entry extension"
complete -c ls -s "Z" -l "context" -d "print any security context of each file"
complete -c ls -s "1" -d "list one file per line."
complete -c ls -l "help" -d "display this help and exit"
complete -c ls -l "version" -d "output version information and exit"
