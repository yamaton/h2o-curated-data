# Auto-generated with h2o

complete -c mkfifo -s "m" -l "mode" -d "set file permission bits to MODE, not a=rw - umask" -r
complete -c mkfifo -s "Z" -d "set the SELinux security context to default type"
complete -c mkfifo -l "context" -d "like -Z, or if CTX is specified then set the SELinux or SMACK security context to CTX" -x
complete -c mkfifo -l "help" -d "display this help and exit"
complete -c mkfifo -l "version" -d "output version information and exit"
