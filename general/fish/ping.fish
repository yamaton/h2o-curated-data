# Auto-generated with h2o

complete -c ping -s "a" -d "use audible ping"
complete -c ping -s "A" -d "use adaptive ping"
complete -c ping -s "B" -d "sticky source address"
complete -c ping -s "c" -d "stop after <count> replies" -x
complete -c ping -s "D" -d "print timestamps"
complete -c ping -s "d" -d "use SO_DEBUG socket option"
complete -c ping -s "f" -d "flood ping"
complete -c ping -s "h" -d "print help and exit"
complete -c ping -s "I" -d "either interface name or address" -x
complete -c ping -s "i" -d "seconds between sending each packet" -x
complete -c ping -s "L" -d "suppress loopback of multicast packets"
complete -c ping -s "l" -d "send <preload> number of packages while waiting replies" -x
complete -c ping -s "m" -d "tag the packets going out" -x
complete -c ping -s "M" -d "define mtu discovery, can be one of <do|dont|want>" -x
complete -c ping -s "n" -d "no dns name resolution"
complete -c ping -s "O" -d "report outstanding replies"
complete -c ping -s "p" -d "contents of padding byte" -x
complete -c ping -s "q" -d "quiet output"
complete -c ping -s "Q" -d "use quality of service <tclass> bits" -x
complete -c ping -s "s" -d "use <size> as number of data bytes to be sent" -x
complete -c ping -s "S" -d "use <size> as SO_SNDBUF socket option value" -x
complete -c ping -s "t" -d "define time to live" -x
complete -c ping -s "U" -d "print user-to-user latency"
complete -c ping -s "v" -d "verbose output"
complete -c ping -s "V" -d "print version and exit"
complete -c ping -s "w" -d "reply wait <deadline> in seconds" -x
complete -c ping -s "W" -d "time to wait for response" -x
complete -c ping -s "4" -d "use IPv4"
complete -c ping -s "b" -d "allow pinging broadcast"
complete -c ping -s "R" -d "record route"
complete -c ping -s "T" -d "define timestamp, can be one of <tsonly|tsandaddr|tsprespec>" -x
complete -c ping -s "6" -d "use IPv6"
complete -c ping -s "F" -d "define flow label, default is random" -x
complete -c ping -s "N" -d "use icmp6 node info query, try <help> as argument" -x
