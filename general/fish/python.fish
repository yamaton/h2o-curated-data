# Auto-generated with h2o

complete -c python -s "B" -d "Don't write .pyc files on import."
complete -c python -s "b" -d "Issue warnings about str(bytes_instance), str(bytearray_instance) and comparing bytes/bytearray with str."
complete -c python -s "c" -d "Specify the command to execute (see next section)." -x
complete -c python -l "check-hash-based-pycs" -d "Configure how Python evaluates the up-to-dateness of hash-based .pyc files." -r
complete -c python -s "d" -d "Turn on parser debugging output (for expert only, depending on compilation options)."
complete -c python -s "E" -d "Ignore environment variables like PYTHONPATH and PYTHONHOME that modify the behavior of the interpreter."
complete -c python -s "h" -s "?" -l "help" -d "Prints the usage for the interpreter executable and exits."
complete -c python -s "i" -d "When a script is passed as first argument or the -c option is used, enter interactive mode after executing the script or the command."
complete -c python -s "I" -d "Run Python in isolated mode."
complete -c python -s "m" -d "Searches sys.path for the named module and runs the corresponding .py file as a script." -r
complete -c python -s "O" -d "Remove assert statements and any code conditional on the value of __debug__; augment the filename for compiled (bytecode) files by adding .opt-1 before the .pyc extension."
complete -c python -o "OO" -d "Do -O and also discard docstrings; change the filename for compiled (bytecode) files by adding .opt-2 before the .pyc extension."
complete -c python -s "q" -d "Do not print the version and copyright messages."
complete -c python -s "s" -d "Don't add user site directory to sys.path."
complete -c python -s "S" -d "Disable the import of the module site and the site-dependent manipulations of sys.path that it entails."
complete -c python -s "u" -d "Force the stdout and stderr streams to be unbuffered."
complete -c python -s "v" -d "Print a message each time a module is initialized, showing the place (filename or built-in module) from which it is loaded."
complete -c python -s "V" -l "version" -d "Prints the Python version number of the executable and exits."
complete -c python -s "W" -d "Warning control." -r
complete -c python -s "X" -d "Set implementation specific option." -x
complete -c python -s "x" -d "Skip the first line of the source."
