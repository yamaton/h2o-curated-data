# Auto-generated with h2o

complete -c shuf -s "e" -l "echo" -d "treat each ARG as an input line"
complete -c shuf -s "i" -l "input-range" -d "treat each number LO through HI as an input line" -x
complete -c shuf -s "n" -l "head-count" -d "output at most COUNT lines" -x
complete -c shuf -s "o" -l "output" -d "write result to FILE instead of standard output" -r
complete -c shuf -l "random-source" -d "get random bytes from FILE" -r
complete -c shuf -s "r" -l "repeat" -d "output lines can be repeated"
complete -c shuf -s "z" -l "zero-terminated" -d "line delimiter is NUL, not newline"
complete -c shuf -l "help" -d "display this help and exit"
complete -c shuf -l "version" -d "output version information and exit"
