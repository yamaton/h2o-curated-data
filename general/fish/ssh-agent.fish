# Auto-generated with h2o

complete -c ssh-agent -s "a" -d "Bind the agent to the UNIX-domain socket bind_address." -r
complete -c ssh-agent -s "c" -d "Generate C-shell commands on stdout."
complete -c ssh-agent -s "D" -d "Foreground mode."
complete -c ssh-agent -s "d" -d "Debug mode."
complete -c ssh-agent -s "E" -d "Specifies the hash algorithm used when displaying key fingerprints." -x
complete -c ssh-agent -s "k" -d "Kill the current agent (given by the SSH_AGENT_PID environment variable)."
complete -c ssh-agent -s "P" -d "Specify a pattern-list of acceptable paths for PKCS#11 provider and FIDO authenticator middleware shared libraries that may be used with the -S or -s options to ssh-add(1)." -r
complete -c ssh-agent -s "s" -d "Generate Bourne shell commands on stdout."
complete -c ssh-agent -s "t" -d "Set a default value for the maximum lifetime of identities added to the agent." -x
