# Auto-generated with h2o

complete -c sudo -s "A" -l "askpass" -d "use a helper program for password prompting"
complete -c sudo -s "b" -l "background" -d "run command in the background"
complete -c sudo -s "B" -l "bell" -d "ring bell when prompting"
complete -c sudo -s "C" -l "close-from" -d "close all file descriptors >= num" -r
complete -c sudo -s "D" -l "chdir" -d "change the working directory before running command" -r
complete -c sudo -s "E" -l "preserve-env" -d "preserve user environment when running command"
complete -c sudo -l "preserve-env" -d "preserve specific environment variables" -x
complete -c sudo -s "e" -l "edit" -d "edit files instead of running a command"
complete -c sudo -s "g" -l "group" -d "run command as the specified group name or ID" -x
complete -c sudo -s "H" -l "set-home" -d "set HOME variable to target user's home dir"
complete -c sudo -s "h" -l "help" -d "display help message and exit"
complete -c sudo -s "h" -l "host" -d "run command on host (if supported by plugin)" -x
complete -c sudo -s "i" -l "login" -d "run login shell as the target user; a command may also be specified"
complete -c sudo -s "K" -l "remove-timestamp" -d "remove timestamp file completely"
complete -c sudo -s "k" -l "reset-timestamp" -d "invalidate timestamp file"
complete -c sudo -s "l" -l "list" -d "list user's privileges or check a specific command; use twice for longer format"
complete -c sudo -s "n" -l "non-interactive" -d "non-interactive mode, no prompts are used"
complete -c sudo -s "P" -l "preserve-groups" -d "preserve group vector instead of setting to target's"
complete -c sudo -s "p" -l "prompt" -d "use the specified password prompt" -x
complete -c sudo -s "R" -l "chroot" -d "change the root directory before running command" -r
complete -c sudo -s "r" -l "role" -d "create SELinux security context with specified role" -x
complete -c sudo -s "S" -l "stdin" -d "read password from standard input"
complete -c sudo -s "s" -l "shell" -d "run shell as the target user; a command may also be specified"
complete -c sudo -s "t" -l "type" -d "create SELinux security context with specified type" -x
complete -c sudo -s "T" -l "command-timeout" -d "terminate command after the specified time limit" -x
complete -c sudo -s "U" -l "other-user" -d "in list mode, display privileges for user" -x
complete -c sudo -s "u" -l "user" -d "run command (or edit file) as specified user name or ID" -r
complete -c sudo -s "V" -l "version" -d "display version information and exit"
complete -c sudo -s "v" -l "validate" -d "update user's timestamp without running a command"
complete -c sudo  -d "stop processing command line arguments"
