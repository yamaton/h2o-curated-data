# Auto-generated with h2o

complete -c xz -s "z" -l "compress" -d "force compression"
complete -c xz -s "d" -l "decompress" -d "force decompression"
complete -c xz -s "t" -l "test" -d "test compressed file integrity"
complete -c xz -s "l" -l "list" -d "list information about .xz files"
complete -c xz -s "k" -l "keep" -d "keep (don't delete) input files"
complete -c xz -s "f" -l "force" -d "force overwrite of output file and (de)compress links"
complete -c xz -s "c" -l "stdout" -d "write to standard output and don't delete input files"
complete -c xz -s "0" -s "1" -s "2" -s "3" -s "4" -s "5" -s "6" -s "7" -s "8" -s "9" -d "compression preset; default is 6; take compressor *and* decompressor memory usage into account before using 7-9!" -x
complete -c xz -s "e" -l "extreme" -d "try to improve compression ratio by using more CPU time; does not affect decompressor memory requirements"
complete -c xz -s "T" -l "threads" -d "use at most NUM threads; the default is 1; set to 0 to use as many threads as there are processor cores" -x
complete -c xz -s "q" -l "quiet" -d "suppress warnings; specify twice to suppress errors too"
complete -c xz -s "v" -l "verbose" -d "be verbose; specify twice for even more verbose"
complete -c xz -s "h" -l "help" -d "display this short help and exit"
complete -c xz -s "H" -l "long-help" -d "display the long help (lists also the advanced options)"
complete -c xz -s "V" -l "version" -d "display the version number and exit"
