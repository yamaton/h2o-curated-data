name: kops
description: kOps is Kubernetes Operations
options:
  - names:
      - --add_dir_header
    argument: ""
    description: If true, adds the file directory to the header of the log messages
  - names:
      - --alsologtostderr
    argument: ""
    description: log to standard error as well as files
  - names:
      - --config
    argument: string
    description: yaml config file (default is $HOME/.kops.yaml)
  - names:
      - -h
      - --help
    argument: ""
    description: help for kops
  - names:
      - --log_backtrace_at
    argument: traceLocation
    description: when logging hits line file:N, emit a stack trace (default :0)
  - names:
      - --log_dir
    argument: string
    description: If non-empty, write log files in this directory
  - names:
      - --log_file
    argument: string
    description: If non-empty, use this log file
  - names:
      - --log_file_max_size
    argument: uint
    description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
  - names:
      - --logtostderr
    argument: ""
    description: log to standard error instead of files (default true)
  - names:
      - --name
    argument: string
    description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
  - names:
      - --one_output
    argument: ""
    description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
  - names:
      - --skip_headers
    argument: ""
    description: If true, avoid header prefixes in the log messages
  - names:
      - --skip_log_headers
    argument: ""
    description: If true, avoid headers when opening log files
  - names:
      - --state
    argument: string
    description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
  - names:
      - --stderrthreshold
    argument: severity
    description: logs at or above this threshold go to stderr (default 2)
  - names:
      - -v
      - --v
    argument: Level
    description: number for the log level verbosity
  - names:
      - --vmodule
    argument: moduleSpec
    description: comma-separated list of pattern=N settings for file-filtered logging
subcommands:
  - name: completion
    description: generate the autocompletion script for the specified shell
    options:
      - names:
          - -h
          - --help
        argument: ""
        description: help for completion
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    subcommands:
      - name: bash
        description: generate the autocompletion script for bash
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for bash
          - names:
              - --no-descriptions
            argument: ""
            description: disable completion descriptions
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: fish
        description: generate the autocompletion script for fish
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for fish
          - names:
              - --no-descriptions
            argument: ""
            description: disable completion descriptions
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: zsh
        description: generate the autocompletion script for zsh
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for zsh
          - names:
              - --no-descriptions
            argument: ""
            description: disable completion descriptions
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
  - name: create
    description: Create a resource by command line, filename or stdin.
    options:
      - names:
          - -f
          - --filename
        argument: strings
        description: Filename to use to create the resource
      - names:
          - -h
          - --help
        argument: ""
        description: help for create
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    subcommands:
      - name: cluster
        description: Create a Kubernetes cluster.
        options:
          - names:
              - --node-count
            argument: "2"
            description: '# Create a cluster in AWS with High Availability masters. This cluster'
          - names:
              - --yes
            argument: ""
            description: '# Create a cluster in Digital Ocean.'
          - names:
              - --yes
            argument: ""
            description: '# Generate a cluster spec to apply later.'
          - names:
              - --admin-access
            argument: strings
            description: Restrict API access to this CIDR. If not set, access will not be restricted by IP. (default [0.0.0.0/0,::/0])
          - names:
              - --api-loadbalancer-class
            argument: string
            description: 'Class of loadbalancer for the Kubernetes API (AWS only): classic or network'
          - names:
              - --api-loadbalancer-type
            argument: string
            description: 'Type of loadbalancer for the Kubernetes API: public or internal'
          - names:
              - --api-ssl-certificate
            argument: string
            description: ARN of the SSL Certificate to use for the Kubernetes API loadbalancer (AWS only)
          - names:
              - --associate-public-ip
            argument: ""
            description: Specify --associate-public-ip=[true|false] to enable/disable association of public IP for master ASG and nodes. Default is 'true'.
          - names:
              - --authorization
            argument: string
            description: 'Authorization mode: AlwaysAllow or RBAC (default "RBAC")'
          - names:
              - --bastion
            argument: ""
            description: Enable a bastion instance group. Only applies to private topology.
          - names:
              - --channel
            argument: string
            description: Channel for default versions and configuration to use (default "stable")
          - names:
              - --cloud
            argument: string
            description: Cloud provider to use - aws, digitalocean, openstack
          - names:
              - --cloud-labels
            argument: string
            description: A list of key/value pairs used to tag all instance groups (for example "Owner=John Doe,Team=Some Team").
          - names:
              - --container-runtime
            argument: string
            description: 'Container runtime to use: containerd, docker'
          - names:
              - --disable-subnet-tags
            argument: ""
            description: Disable automatic subnet tagging
          - names:
              - --discovery-store
            argument: string
            description: A public location where we publish OIDC-compatible discovery information under a cluster-specific directory. Enables IRSA in AWS.
          - names:
              - --dns
            argument: string
            description: 'DNS type to use: public or private (default "Public")'
          - names:
              - --dns-zone
            argument: string
            description: DNS hosted zone (defaults to longest matching zone)
          - names:
              - --dry-run
            argument: ""
            description: If true, only print the object that would be sent, without sending it. This flag can be used to create a cluster YAML or JSON manifest.
          - names:
              - --encrypt-etcd-storage
            argument: ""
            description: Generate key in AWS KMS and use it for encrypt etcd volumes
          - names:
              - --etcd-storage-type
            argument: string
            description: The default storage type for etcd members
          - names:
              - --gce-service-account
            argument: string
            description: 'Service account with which the GCE VM runs. Warning: if not set, VMs will run as default compute service account.'
          - names:
              - -h
              - --help
            argument: ""
            description: help for cluster
          - names:
              - --image
            argument: string
            description: Machine image for all instances
          - names:
              - --kubernetes-version
            argument: string
            description: Version of kubernetes to run (defaults to version in channel)
          - names:
              - --master-count
            argument: int32
            description: Number of masters. Defaults to one master per master-zone
          - names:
              - --master-image
            argument: string
            description: Machine image for masters. Takes precedence over --image
          - names:
              - --master-public-name
            argument: string
            description: Domain name of the public Kubernetes API
          - names:
              - --master-security-groups
            argument: strings
            description: Additional precreated security groups to add to masters.
          - names:
              - --master-size
            argument: string
            description: Machine type for masters
          - names:
              - --master-tenancy
            argument: string
            description: 'Tenancy of the master group (AWS only): default or dedicated'
          - names:
              - --master-volume-size
            argument: int32
            description: Instance volume size (in GB) for masters
          - names:
              - --master-zones
            argument: strings
            description: Zones in which to run masters (must be an odd number)
          - names:
              - --network-cidr
            argument: string
            description: Network CIDR to use
          - names:
              - --networking
            argument: string
            description: Networking mode. kubenet, external, weave, flannel-vxlan (or flannel), flannel-udp, calico, canal, kube-router, amazonvpc, cilium, cilium-etcd, cni. (default "kubenet")
          - names:
              - --node-count
            argument: int32
            description: Total number of worker nodes. Defaults to one node per zone
          - names:
              - --node-image
            argument: string
            description: Machine image for worker nodes. Takes precedence over --image
          - names:
              - --node-security-groups
            argument: strings
            description: Additional precreated security groups to add to worker nodes.
          - names:
              - --node-size
            argument: string
            description: Machine type for worker nodes
          - names:
              - --node-tenancy
            argument: string
            description: 'Tenancy of the node group (AWS only): default or dedicated'
          - names:
              - --node-volume-size
            argument: int32
            description: Instance volume size (in GB) for worker nodes
          - names:
              - --os-dns-servers
            argument: string
            description: comma separated list of DNS Servers which is used in network
          - names:
              - --os-ext-net
            argument: string
            description: External network to use with the openstack router
          - names:
              - --os-ext-subnet
            argument: string
            description: External floating subnet to use with the openstack router
          - names:
              - --os-kubelet-ignore-az
            argument: ""
            description: Attach volumes across availability zones
          - names:
              - --os-lb-floating-subnet
            argument: string
            description: External subnet to use with the kubernetes api
          - names:
              - --os-network
            argument: string
            description: ID of the existing OpenStack network to use
          - names:
              - --os-octavia
            argument: ""
            description: Use octavia loadbalancer API
          - names:
              - --os-octavia-provider
            argument: string
            description: Octavia provider to use
          - names:
              - --out
            argument: string
            description: Path to write any local output
          - names:
              - -o
              - --output
            argument: string
            description: Output format. One of json or yaml. Used with the --dry-run flag.
          - names:
              - --project
            argument: string
            description: Project to use (must be set on GCE)
          - names:
              - --ssh-access
            argument: strings
            description: Restrict SSH access to this CIDR. If not set, uses the value of the admin-access flag.
          - names:
              - --ssh-public-key
            argument: string
            description: SSH public key to use
          - names:
              - --subnets
            argument: strings
            description: Shared subnets to use
          - names:
              - --target
            argument: string
            description: 'Valid targets: direct, terraform, cloudformation. Set this flag to terraform if you want kOps to generate terraform (default "direct")'
          - names:
              - -t
              - --topology
            argument: string
            description: 'Network topology for the cluster: public or private (default "public")'
          - names:
              - --utility-subnets
            argument: strings
            description: Shared utility subnets to use
          - names:
              - --vpc
            argument: string
            description: Shared VPC to use
          - names:
              - -y
              - --yes
            argument: ""
            description: Specify --yes to immediately create the cluster
          - names:
              - --zones
            argument: strings
            description: Zones in which to run the cluster
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: instancegroup
        description: Create an instancegroup
        aliases:
          - ig
          - instancegroups
        options:
          - names:
              - --role
              - --subnet
            argument: node
            description: '# Create a YAML manifest for an instancegroup for the k8s-cluster.example.com cluster.'
          - names:
              - --dry-run
            argument: ""
            description: Only print the object that would be created, without created it. This flag can be used to create an instance group YAML or JSON manifest.
          - names:
              - --edit
            argument: ""
            description: Open an editor to edit default values (default true)
          - names:
              - -h
              - --help
            argument: ""
            description: help for instancegroup
          - names:
              - -o
              - --output
            argument: string
            description: Output format. One of json or yaml
          - names:
              - --role
            argument: string
            description: Type of instance group to create (master,node,bastion) (default "Node")
          - names:
              - --subnet
            argument: strings
            description: Subnet in which to create instance group. One of Availability Zone like eu-west-1a or a comma-separated list of multiple Availability Zones.
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: keypair
        description: Add a CA certificate and private key to a keyset.
        options:
          - names:
              - --cert
            argument: string
            description: Path to CA certificate
          - names:
              - -h
              - --help
            argument: ""
            description: help for keypair
          - names:
              - --key
            argument: string
            description: Path to CA private key
          - names:
              - --primary
            argument: ""
            description: Make the keypair the one used to issue certificates
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: secret
        description: Create a secret.
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for secret
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
        subcommands:
          - name: ciliumpassword
            description: Create a Cilium IPsec configuration.
            options:
              - names:
                  - --name
                  - --state
                argument: k8s-cluster.example.com
                description: '# Create a new Cilium IPsec key via stdin.'
              - names:
                  - -f
                  - --filename
                argument: string
                description: Path to the Cilium IPsec configuration file
              - names:
                  - --force
                argument: ""
                description: Force replace the secret if it already exists
              - names:
                  - -h
                  - --help
                argument: ""
                description: help for ciliumpassword
              - names:
                  - --add_dir_header
                argument: ""
                description: If true, adds the file directory to the header of the log messages
              - names:
                  - --alsologtostderr
                argument: ""
                description: log to standard error as well as files
              - names:
                  - --config
                argument: string
                description: yaml config file (default is $HOME/.kops.yaml)
              - names:
                  - --log_backtrace_at
                argument: traceLocation
                description: when logging hits line file:N, emit a stack trace (default :0)
              - names:
                  - --log_dir
                argument: string
                description: If non-empty, write log files in this directory
              - names:
                  - --log_file
                argument: string
                description: If non-empty, use this log file
              - names:
                  - --log_file_max_size
                argument: uint
                description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
              - names:
                  - --logtostderr
                argument: ""
                description: log to standard error instead of files (default true)
              - names:
                  - --name
                argument: string
                description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
              - names:
                  - --one_output
                argument: ""
                description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
              - names:
                  - --skip_headers
                argument: ""
                description: If true, avoid header prefixes in the log messages
              - names:
                  - --skip_log_headers
                argument: ""
                description: If true, avoid headers when opening log files
              - names:
                  - --state
                argument: string
                description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
              - names:
                  - --stderrthreshold
                argument: severity
                description: logs at or above this threshold go to stderr (default 2)
              - names:
                  - -v
                  - --v
                argument: Level
                description: number for the log level verbosity
              - names:
                  - --vmodule
                argument: moduleSpec
                description: comma-separated list of pattern=N settings for file-filtered logging
          - name: dockerconfig
            description: Create a Docker config.
            options:
              - names:
                  - --name
                  - --state
                argument: k8s-cluster.example.com
                description: '# Create a docker config via stdin.'
              - names:
                  - --name
                  - --state
                argument: k8s-cluster.example.com
                description: '# Replace an existing docker config secret.'
              - names:
                  - -f
                  - --filename
                argument: string
                description: Path to Docker config JSON file
              - names:
                  - --force
                argument: ""
                description: Force replace the secret if it already exists
              - names:
                  - -h
                  - --help
                argument: ""
                description: help for dockerconfig
              - names:
                  - --add_dir_header
                argument: ""
                description: If true, adds the file directory to the header of the log messages
              - names:
                  - --alsologtostderr
                argument: ""
                description: log to standard error as well as files
              - names:
                  - --config
                argument: string
                description: yaml config file (default is $HOME/.kops.yaml)
              - names:
                  - --log_backtrace_at
                argument: traceLocation
                description: when logging hits line file:N, emit a stack trace (default :0)
              - names:
                  - --log_dir
                argument: string
                description: If non-empty, write log files in this directory
              - names:
                  - --log_file
                argument: string
                description: If non-empty, use this log file
              - names:
                  - --log_file_max_size
                argument: uint
                description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
              - names:
                  - --logtostderr
                argument: ""
                description: log to standard error instead of files (default true)
              - names:
                  - --name
                argument: string
                description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
              - names:
                  - --one_output
                argument: ""
                description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
              - names:
                  - --skip_headers
                argument: ""
                description: If true, avoid header prefixes in the log messages
              - names:
                  - --skip_log_headers
                argument: ""
                description: If true, avoid headers when opening log files
              - names:
                  - --state
                argument: string
                description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
              - names:
                  - --stderrthreshold
                argument: severity
                description: logs at or above this threshold go to stderr (default 2)
              - names:
                  - -v
                  - --v
                argument: Level
                description: number for the log level verbosity
              - names:
                  - --vmodule
                argument: moduleSpec
                description: comma-separated list of pattern=N settings for file-filtered logging
          - name: weavepassword
            description: Create a Weave password.
            options:
              - names:
                  - --name
                  - --state
                argument: k8s-cluster.example.com
                description: '# Install a specific weave password.'
              - names:
                  - --name
                  - --state
                argument: k8s-cluster.example.com
                description: '# Install a specific weave password via stdin.'
              - names:
                  - --name
                  - --state
                argument: k8s-cluster.example.com
                description: '# Replace an existing weave password.'
              - names:
                  - -f
                  - --filename
                argument: string
                description: Path to Weave password file
              - names:
                  - --force
                argument: ""
                description: Force replace the secret if it already exists
              - names:
                  - -h
                  - --help
                argument: ""
                description: help for weavepassword
              - names:
                  - --add_dir_header
                argument: ""
                description: If true, adds the file directory to the header of the log messages
              - names:
                  - --alsologtostderr
                argument: ""
                description: log to standard error as well as files
              - names:
                  - --config
                argument: string
                description: yaml config file (default is $HOME/.kops.yaml)
              - names:
                  - --log_backtrace_at
                argument: traceLocation
                description: when logging hits line file:N, emit a stack trace (default :0)
              - names:
                  - --log_dir
                argument: string
                description: If non-empty, write log files in this directory
              - names:
                  - --log_file
                argument: string
                description: If non-empty, use this log file
              - names:
                  - --log_file_max_size
                argument: uint
                description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
              - names:
                  - --logtostderr
                argument: ""
                description: log to standard error instead of files (default true)
              - names:
                  - --name
                argument: string
                description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
              - names:
                  - --one_output
                argument: ""
                description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
              - names:
                  - --skip_headers
                argument: ""
                description: If true, avoid header prefixes in the log messages
              - names:
                  - --skip_log_headers
                argument: ""
                description: If true, avoid headers when opening log files
              - names:
                  - --state
                argument: string
                description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
              - names:
                  - --stderrthreshold
                argument: severity
                description: logs at or above this threshold go to stderr (default 2)
              - names:
                  - -v
                  - --v
                argument: Level
                description: number for the log level verbosity
              - names:
                  - --vmodule
                argument: moduleSpec
                description: comma-separated list of pattern=N settings for file-filtered logging
  - name: delete
    description: Delete clusters, instancegroups, instances, and secrets.
    options:
      - names:
          - -f
          - --filename
        argument: strings
        description: Filename to use to delete the resource
      - names:
          - -h
          - --help
        argument: ""
        description: help for delete
      - names:
          - -y
          - --yes
        argument: ""
        description: Specify --yes to immediately delete the resource
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    subcommands:
      - name: cluster
        description: Delete a cluster.
        options:
          - names:
              - --external
            argument: ""
            description: Delete an external cluster
          - names:
              - -h
              - --help
            argument: ""
            description: help for cluster
          - names:
              - --region
            argument: string
            description: External cluster's cloud region
          - names:
              - --unregister
            argument: ""
            description: Don't delete cloud resources, just unregister the cluster
          - names:
              - -y
              - --yes
            argument: ""
            description: Specify --yes to delete the cluster
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: instance
        description: Delete an instance.
        options:
          - names:
              - --cloudonly
            argument: ""
            description: Perform deletion update without confirming progress with Kubernetes
          - names:
              - --fail-on-drain-error
            argument: ""
            description: Fail if draining a node fails (default true)
          - names:
              - --fail-on-validate-error
            argument: ""
            description: Fail if the cluster fails to validate (default true)
          - names:
              - -h
              - --help
            argument: ""
            description: help for instance
          - names:
              - --post-drain-delay
            argument: duration
            description: Time to wait after draining each node (default 5s)
          - names:
              - --surge
            argument: ""
            description: Surge by detaching the node from the ASG before deletion (default true)
          - names:
              - --validate-count
            argument: int32
            description: Number of times that a cluster needs to be validated after single node update (default 2)
          - names:
              - --validation-timeout
            argument: duration
            description: Maximum time to wait for a cluster to validate (default 15m0s)
          - names:
              - -y
              - --yes
            argument: ""
            description: Specify --yes to immediately delete the instance
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: instancegroup
        aliases:
          - ig
          - instancegroups
        description: Delete instance group
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for instancegroup
          - names:
              - -y
              - --yes
            argument: ""
            description: Specify --yes to immediately delete the instance group
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: secret
        description: Delete one or more secrets.
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for secret
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: sshpublickey
        aliases:
          - ssh
          - sshpublickeys
        description: Delete an SSH public key
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for edit
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
  - name: distrust
    description: Distrust keypairs.
    options:
      - names:
          - -h
          - --help
        argument: ""
        description: help for distrust
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    positionalArguments:
      - name: keypair
        description: Distrust a keypair
  - name: edit
    description: Edit clusters and other resources.
    options:
      - names:
          - -h
          - --help
        argument: ""
        description: help for edit
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    subcommands:
      - name: cluster
        description: Edit cluster.
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for cluster
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: instancegroup
        aliases:
          - ig
          - instancegroups
        description: Edit instancegroup
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for cluster
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
  - name: export
    description: Export configuration.
    options:
      - names:
          - -h
          - --help
        argument: ""
        description: help for export
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    subcommands:
      - name: kubeconfig
        aliases:
          - kubecfg
        description: Export kubeconfig
        options:
          - names:
              - --admin
            argument: duration[=18h0m0s]
            description: Also export a cluster admin user credential with the specified lifetime and add it to the cluster context
          - names:
              - --all
            argument: ""
            description: Export all clusters from the kOps state store
          - names:
              - --auth-plugin
            argument: ""
            description: Use the kOps authentication plugin
          - names:
              - -h
              - --help
            argument: ""
            description: help for kubeconfig
          - names:
              - --internal
            argument: ""
            description: Use the cluster's internal DNS name
          - names:
              - --kubeconfig
            argument: string
            description: Filename of the kubeconfig to create
          - names:
              - --user
            argument: string
            description: Existing user in kubeconfig file to use
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
  - name: get
    description: Get one or many resources.
    options:
      - names:
          - -h
          - --help
        argument: ""
        description: help for get
      - names:
          - -o
          - --output
        argument: string
        description: 'output format. One of: table, yaml, json (default "table")'
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    subcommands:
      - name: assets
        description: Display assets for cluster.
        options:
          - names:
              - --copy
            argument: ""
            description: copy assets to local repository
          - names:
              - -h
              - --help
            argument: ""
            description: help for assets
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - -o
              - --output
            argument: string
            description: 'output format. One of: table, yaml, json (default "table")'
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: clusters
        description: Get one or many clusters.
        options:
          - names:
              - --full
            argument: ""
            description: Show fully populated configuration
          - names:
              - -h
              - --help
            argument: ""
            description: help for clusters
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - -o
              - --output
            argument: string
            description: 'output format. One of: table, yaml, json (default "table")'
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: instances
        description: Display cluster instances.
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for instances
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - -o
              - --output
            argument: string
            description: 'output format. One of: table, yaml, json (default "table")'
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: instancegroup
        aliases:
          - ig
          - instancegroups
        description: Display cluster instances
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for promote
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: keypairs
        description: Get one or many keypairs.
        options:
          - names:
              - --distrusted
            argument: ""
            description: Include distrusted keypairs
          - names:
              - -h
              - --help
            argument: ""
            description: help for keypairs
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - -o
              - --output
            argument: string
            description: 'output format. One of: table, yaml, json (default "table")'
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: secrets
        description: Get one or many secrets.
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for secrets
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - -o
              - --output
            argument: string
            description: 'output format. One of: table, yaml, json (default "table")'
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: sshpublickey
        aliases:
          - ssh
          - sshpublickeys
        description: Get one or many secrets
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for secrets
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - -o
              - --output
            argument: string
            description: 'output format. One of: table, yaml, json (default "table")'
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
  - name: promote
    description: Promote a resource.
    options:
      - names:
          - -h
          - --help
        argument: ""
        description: help for promote
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    positionalArguments:
      - name: keypair
        description: Promote a keypair to be the primary, used for signing.
  - name: replace
    description: Replace cluster resources.
    options:
      - names:
          - -f
          - --filename
        argument: strings
        description: A list of one or more files separated by a comma.
      - names:
          - --force
        argument: ""
        description: Force any changes, which will also create any non-existing resource
      - names:
          - -h
          - --help
        argument: ""
        description: help for replace
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
  - name: rolling-update
    description: Rolling update a cluster.
    options:
      - names:
          - -h
          - --help
        argument: ""
        description: help for rolling-update
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    subcommands:
      - name: cluster
        description: Rolling update a cluster.
        options:
          - names:
              - --fail-on-validate-error
            argument: '"false"'
            description: '# Update the k8s-cluster.example.com kOps cluster.'
          - names:
              - --bastion-interval
            argument: duration
            description: Time to wait between restarting bastions (default 15s)
          - names:
              - --cloudonly
            argument: ""
            description: Perform rolling update without confirming progress with Kubernetes
          - names:
              - --drain-timeout
            argument: duration
            description: Maximum time to wait for a node to drain (default 15m0s)
          - names:
              - --fail-on-drain-error
            argument: ""
            description: Fail if draining a node fails (default true)
          - names:
              - --fail-on-validate-error
            argument: ""
            description: Fail if the cluster fails to validate (default true)
          - names:
              - --force
            argument: ""
            description: Force rolling update, even if no changes
          - names:
              - -h
              - --help
            argument: ""
            description: help for cluster
          - names:
              - --instance-group
            argument: strings
            description: Instance groups to update (defaults to all if not specified)
          - names:
              - --instance-group-roles
            argument: strings
            description: Instance group roles to update (master,apiserver,node,bastion)
          - names:
              - -i
              - --interactive
            argument: ""
            description: Prompt to continue after each instance is updated
          - names:
              - --master-interval
            argument: duration
            description: Time to wait between restarting control plane nodes (default 15s)
          - names:
              - --node-interval
            argument: duration
            description: Time to wait between restarting worker nodes (default 15s)
          - names:
              - --post-drain-delay
            argument: duration
            description: Time to wait after draining each node (default 5s)
          - names:
              - --validate-count
            argument: int32
            description: Number of times that a cluster needs to be validated after single node update (default 2)
          - names:
              - --validation-timeout
            argument: duration
            description: Maximum time to wait for a cluster to validate (default 15m0s)
          - names:
              - -y
              - --yes
            argument: ""
            description: Perform rolling update immediately; without --yes rolling-update executes a dry-run
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
  - name: toolbox
    description: Miscellaneous, infrequently used commands.
    options:
      - names:
          - -h
          - --help
        argument: ""
        description: help for toolbox
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    subcommands:
      - name: dump
        description: Dump cluster information
        options:
          - names:
              - --dir
            argument: string
            description: Target directory; if specified will collect logs and other information.
          - names:
              - -h
              - --help
            argument: ""
            description: help for dump
          - names:
              - -o
              - --output
            argument: string
            description: Output format. One of json or yaml (default "yaml")
          - names:
              - --private-key
            argument: string
            description: File containing private key to use for SSH access to instances (default "~/.ssh/id_rsa")
          - names:
              - --ssh-user
            argument: string
            description: The remote user for SSH access to instances (default "ubuntu")
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
      - name: template
        description: Generate cluster.yaml from template
        options:
          - names:
              - --channel
            argument: string
            description: Channel to use for the channel* functions (default "stable")
          - names:
              - --config-value
            argument: string
            description: Show the value of a specific configuration value
          - names:
              - --fail-on-missing
            argument: ""
            description: Fail on referencing unset variables in templates (default true)
          - names:
              - --format-yaml
            argument: ""
            description: Attempt to format the generated yaml content before output
          - names:
              - -h
              - --help
            argument: ""
            description: help for template
          - names:
              - --out
            argument: string
            description: Path to output file. Defaults to stdout
          - names:
              - --set
            argument: stringArray
            description: 'Set values on the command line (can specify multiple or separate values with commas: key1=val1,key2=val2)'
          - names:
              - --set-string
            argument: stringArray
            description: 'Set STRING values on the command line (can specify multiple or separate values with commas: key1=val1,key2=val2)'
          - names:
              - --snippets
            argument: strings
            description: Path to directory containing snippets used for templating
          - names:
              - --template
            argument: strings
            description: Path to template file or directory of templates to render
          - names:
              - --values
            argument: strings
            description: Path to a configuration file containing values to include in template
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
  - name: trust
    description: Trust keypairs.
    options:
      - names:
          - -h
          - --help
        argument: ""
        description: help for trust
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    subcommands:
      - name: keypair
        description: Trust a keypair.
        options:
          - names:
              - -h
              - --help
            argument: ""
            description: help for keypair
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
  - name: update
    description: Update a cluster.
    options:
      - names:
          - -h
          - --help
        argument: ""
        description: help for update
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    subcommands:
      - name: cluster
        description: Update a cluster.
        options:
          - names:
              - --admin
            argument: duration[=18h0m0s]
            description: Also export a cluster admin user credential with the specified lifetime and add it to the cluster context
          - names:
              - --allow-kops-downgrade
            argument: ""
            description: Allow an older version of kOps to update the cluster than last used
          - names:
              - --create-kube-config
            argument: ""
            description: Will control automatically creating the kube config file on your local filesystem (default true)
          - names:
              - -h
              - --help
            argument: ""
            description: help for cluster
          - names:
              - --internal
            argument: ""
            description: Use the cluster's internal DNS name. Implies --create-kube-config
          - names:
              - --lifecycle-overrides
            argument: strings
            description: 'comma separated list of phase overrides, example: SecurityGroups=Ignore,InternetGateway=ExistsAndWarnIfChanges'
          - names:
              - --out
            argument: string
            description: Path to write any local output
          - names:
              - --phase
            argument: string
            description: 'Subset of tasks to run: cluster, network, security'
          - names:
              - --ssh-public-key
            argument: string
            description: 'SSH public key to use (deprecated: use kops create secret instead)'
          - names:
              - --target
            argument: string
            description: Target - direct, terraform, cloudformation (default "direct")
          - names:
              - --user
            argument: string
            description: Existing user in kubeconfig file to use. Implies --create-kube-config
          - names:
              - -y
              - --yes
            argument: ""
            description: Create cloud resources, without --yes update is in dry run mode
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
  - name: upgrade
    description: Upgrade a kubernetes cluster.
    options:
      - names:
          - -h
          - --help
        argument: ""
        description: help for upgrade
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    subcommands:
      - name: cluster
        description: Upgrade a kubernetes cluster.
        options:
          - names:
              - --channel
            argument: string
            description: Channel to use for upgrade
          - names:
              - -h
              - --help
            argument: ""
            description: help for cluster
          - names:
              - -y
              - --yes
            argument: ""
            description: Apply update
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
  - name: validate
    description: Validate a kOps cluster.
    options:
      - names:
          - -h
          - --help
        argument: ""
        description: help for validate
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
    subcommands:
      - name: cluster
        description: Validate a kOps cluster.
        options:
          - names:
              - --count
            argument: int
            description: Number of consecutive successful validations required
          - names:
              - -h
              - --help
            argument: ""
            description: help for cluster
          - names:
              - --kubeconfig
            argument: string
            description: Path to the kubeconfig file
          - names:
              - -o
              - --output
            argument: string
            description: Output format. One of json|yaml|table. (default "table")
          - names:
              - --wait
            argument: duration
            description: Amount of time to wait for the cluster to become ready
          - names:
              - --add_dir_header
            argument: ""
            description: If true, adds the file directory to the header of the log messages
          - names:
              - --alsologtostderr
            argument: ""
            description: log to standard error as well as files
          - names:
              - --config
            argument: string
            description: yaml config file (default is $HOME/.kops.yaml)
          - names:
              - --log_backtrace_at
            argument: traceLocation
            description: when logging hits line file:N, emit a stack trace (default :0)
          - names:
              - --log_dir
            argument: string
            description: If non-empty, write log files in this directory
          - names:
              - --log_file
            argument: string
            description: If non-empty, use this log file
          - names:
              - --log_file_max_size
            argument: uint
            description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
          - names:
              - --logtostderr
            argument: ""
            description: log to standard error instead of files (default true)
          - names:
              - --name
            argument: string
            description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
          - names:
              - --one_output
            argument: ""
            description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
          - names:
              - --skip_headers
            argument: ""
            description: If true, avoid header prefixes in the log messages
          - names:
              - --skip_log_headers
            argument: ""
            description: If true, avoid headers when opening log files
          - names:
              - --state
            argument: string
            description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
          - names:
              - --stderrthreshold
            argument: severity
            description: logs at or above this threshold go to stderr (default 2)
          - names:
              - -v
              - --v
            argument: Level
            description: number for the log level verbosity
          - names:
              - --vmodule
            argument: moduleSpec
            description: comma-separated list of pattern=N settings for file-filtered logging
  - name: version
    description: Print the kOps version information.
    options:
      - names:
          - -h
          - --help
        argument: ""
        description: help for version
      - names:
          - --short
        argument: ""
        description: only print the main kOps version. Useful for scripting.
      - names:
          - --add_dir_header
        argument: ""
        description: If true, adds the file directory to the header of the log messages
      - names:
          - --alsologtostderr
        argument: ""
        description: log to standard error as well as files
      - names:
          - --config
        argument: string
        description: yaml config file (default is $HOME/.kops.yaml)
      - names:
          - --log_backtrace_at
        argument: traceLocation
        description: when logging hits line file:N, emit a stack trace (default :0)
      - names:
          - --log_dir
        argument: string
        description: If non-empty, write log files in this directory
      - names:
          - --log_file
        argument: string
        description: If non-empty, use this log file
      - names:
          - --log_file_max_size
        argument: uint
        description: Defines the maximum size a log file can grow to. Unit is megabytes. If the value is 0, the maximum file size is unlimited. (default 1800)
      - names:
          - --logtostderr
        argument: ""
        description: log to standard error instead of files (default true)
      - names:
          - --name
        argument: string
        description: Name of cluster. Overrides KOPS_CLUSTER_NAME environment variable
      - names:
          - --one_output
        argument: ""
        description: If true, only write logs to their native severity level (vs also writing to each lower severity level)
      - names:
          - --skip_headers
        argument: ""
        description: If true, avoid header prefixes in the log messages
      - names:
          - --skip_log_headers
        argument: ""
        description: If true, avoid headers when opening log files
      - names:
          - --state
        argument: string
        description: Location of state storage (kops 'config' file). Overrides KOPS_STATE_STORE environment variable
      - names:
          - --stderrthreshold
        argument: severity
        description: logs at or above this threshold go to stderr (default 2)
      - names:
          - -v
          - --v
        argument: Level
        description: number for the log level verbosity
      - names:
          - --vmodule
        argument: moduleSpec
        description: comma-separated list of pattern=N settings for file-filtered logging
version: Version 1.23.2

tldr: |
  > Create, destroy, upgrade and maintain Kubernetes clusters from the command-line.
  > More information: <https://github.com/kubernetes/kops/>.

  - Create a cluster from the configuration specification:

  `kops create cluster -f {{cluster_name.yaml}}`

  - Create a new ssh public key:

  `kops create secret sshpublickey {{key_name}} -i {{~/.ssh/id_rsa.pub}}`

  - Export the cluster configuration to the `~/.kube/config` file:

  `kops export kubecfg {{cluster_name}}`

  - Get the cluster configuration as YAML:

  `kops get cluster {{cluster_name}} -o yaml`

  - Delete a cluster:

  `kops delete cluster {{cluster_name}} --yes`
