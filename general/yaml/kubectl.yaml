name: kubectl
description: kubectl
usage: kubectl [flags] [options]
options: []
subcommands:
  - name: create
    description: Create a resource from a file or from stdin
    usage: kubectl create -f FILENAME [options]
    options:
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - --edit
        argument: "false"
        description: Edit the API resource before creating
      - names:
          - --field-manager
        argument: '''kubectl-create'''
        description: Name of the manager used to track field ownership.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: Filename, directory, or URL to files to use to create the resource
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process the kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
      - names:
          - --raw
        argument: ''''''
        description: Raw URI to POST to the server. Uses the transport specified by the kubeconfig file.
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - --save-config
        argument: "false"
        description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - names:
          - --validate
        argument: '''strict'''
        description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - names:
          - --windows-line-endings
        argument: "false"
        description: Only relevant if --edit=true. Defaults to the line ending native to your platform.
    subcommands:
      - name: clusterrole
        description: Create a cluster role
        usage: kubectl create clusterrole NAME --verb=verb --resource=resource.group [--resource-name=resourcename] [--dry-run=server|client|none] [options]
        options:
          - names:
              - --aggregation-rule
            argument: ""
            description: An aggregation label selector for combining ClusterRoles.
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - --non-resource-url
            argument: '[]'
            description: A partial url that user should have access to.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --resource
            argument: '[]'
            description: Resource that the rule applies to
          - names:
              - --resource-name
            argument: '[]'
            description: Resource in the white list that the rule applies to, repeat this flag for multiple items
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
          - names:
              - --verb
            argument: '[]'
            description: Verb that applies to the resources contained in the rule
      - name: clusterrolebinding
        description: Create a cluster role binding for a particular cluster role
        usage: kubectl create clusterrolebinding NAME --clusterrole=NAME [--user=username] [--group=groupname] [--serviceaccount=namespace:serviceaccountname] [--dry-run=server|client|none] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --clusterrole
            argument: ''''''
            description: ClusterRole this ClusterRoleBinding should reference
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - --group
            argument: '[]'
            description: Groups to bind to the clusterrole. The flag can be repeated to add multiple groups.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --serviceaccount
            argument: '[]'
            description: Service accounts to bind to the clusterrole, in the format <namespace>:<name>. The flag can be repeated to add multiple service accounts.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - name: configmap
        description: Create a config map from a local file, directory or literal value
        usage: kubectl create configmap NAME [--from-file=[key=]source] [--from-literal=key1=value1] [--dry-run=server|client|none] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --append-hash
            argument: "false"
            description: Append a hash of the configmap to its name.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - --from-env-file
            argument: '[]'
            description: Specify the path to a file to read lines of key=val pairs to create a configmap.
          - names:
              - --from-file
            argument: '[]'
            description: Key file can be specified using its file path, in which case file basename will be used as configmap key, or optionally with a key and file path, in which case the given key will be used. Specifying a directory will iterate each named file in the directory whose basename is a valid configmap key.
          - names:
              - --from-literal
            argument: '[]'
            description: Specify a key and literal value to insert in configmap (i.e. mykey=somevalue)
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - name: cronjob
        description: Create a cron job with the specified name
        usage: kubectl create cronjob NAME --image=image --schedule='0/5 * * * ?' -- [COMMAND] [args...] [flags] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - --image
            argument: ''''''
            description: Image name to run.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --restart
            argument: ''''''
            description: 'job''s restart policy. supported values: OnFailure, Never'
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --schedule
            argument: ''''''
            description: A schedule in the Cron format the job should be run with.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - name: deployment
        description: Create a deployment with the specified name
        usage: kubectl create deployment NAME --image=image -- [COMMAND] [args...] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - --image
            argument: '[]'
            description: Image names to run.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --port
            argument: "-1"
            description: The port that this container exposes.
          - names:
              - -r
              - --replicas
            argument: "1"
            description: Number of replicas to create. Default is 1.
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - name: ingress
        description: Create an ingress with the specified name
        usage: kubectl create ingress NAME --rule=host/path=service:port[,tls[=secret]]  [options]
        options:
          - names:
              - --rule
            argument: '"foo.com/*=svc:8080,tls=secret1"'
            description: '# Create an ingress with a default backend'
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --annotation
            argument: '[]'
            description: Annotation to insert in the ingress object, in the format annotation=value
          - names:
              - --class
            argument: ''''''
            description: Ingress Class to be used
          - names:
              - --default-backend
            argument: ''''''
            description: Default service for backend, in format of svcname:port
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --rule
            argument: '[]'
            description: Rule in format host/path=service:port[,tls=secretname]. Paths containing the leading character '*' are considered pathType=Prefix. tls argument is optional.
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - name: job
        description: Create a job with the specified name
        usage: kubectl create job NAME --image=image [--from=cronjob/name] -- [COMMAND] [args...] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - --from
            argument: ''''''
            description: The name of the resource to create a Job from (only cronjob is supported).
          - names:
              - --image
            argument: ''''''
            description: Image name to run.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - name: namespace
        description: Create a namespace with the specified name
        usage: kubectl create namespace NAME [--dry-run=server|client|none] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - name: poddisruptionbudget
        description: Create a pod disruption budget with the specified name
        usage: kubectl create poddisruptionbudget NAME --selector=SELECTOR --min-available=N [--dry-run=server|client|none] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - --max-unavailable
            argument: ''''''
            description: The maximum number or percentage of unavailable pods this budget requires.
          - names:
              - --min-available
            argument: ''''''
            description: The minimum number or percentage of available pods this budget requires.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --selector
            argument: ''''''
            description: A label selector to use for this budget. Only equality-based selector requirements are supported.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - name: priorityclass
        description: Create a priority class with the specified name
        usage: kubectl create priorityclass NAME --value=VALUE --global-default=BOOL [--dry-run=server|client|none] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --description
            argument: ''''''
            description: description is an arbitrary string that usually provides guidelines on when this priority class should be used.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - --global-default
            argument: "false"
            description: global-default specifies whether this PriorityClass should be considered as the default priority.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --preemption-policy
            argument: '''PreemptLowerPriority'''
            description: preemption-policy is the policy for preempting pods with lower priority.
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
          - names:
              - --value
            argument: "0"
            description: the value of this priority class.
      - name: quota
        description: Create a quota with the specified name
        usage: kubectl create quota NAME [--hard=key1=value1,key2=value2] [--scopes=Scope1,Scope2] [--dry-run=server|client|none] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - --hard
            argument: ''''''
            description: A comma-delimited set of resource=quantity pairs that define a hard limit.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --scopes
            argument: ''''''
            description: A comma-delimited set of quota scopes that must all match each object tracked by the quota.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - name: role
        description: Create a role with single rule
        usage: kubectl create role NAME --verb=verb --resource=resource.group/subresource [--resource-name=resourcename] [--dry-run=server|client|none] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --resource
            argument: '[]'
            description: Resource that the rule applies to
          - names:
              - --resource-name
            argument: '[]'
            description: Resource in the white list that the rule applies to, repeat this flag for multiple items
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
          - names:
              - --verb
            argument: '[]'
            description: Verb that applies to the resources contained in the rule
      - name: rolebinding
        description: Create a role binding for a particular role or cluster role
        usage: kubectl create rolebinding NAME --clusterrole=NAME|--role=NAME [--user=username] [--group=groupname] [--serviceaccount=namespace:serviceaccountname] [--dry-run=server|client|none] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --clusterrole
            argument: ''''''
            description: ClusterRole this RoleBinding should reference
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - --group
            argument: '[]'
            description: Groups to bind to the role. The flag can be repeated to add multiple groups.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --role
            argument: ''''''
            description: Role this RoleBinding should reference
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --serviceaccount
            argument: '[]'
            description: Service accounts to bind to the role, in the format <namespace>:<name>. The flag can be repeated to add multiple service accounts.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - name: secret
        description: Create a secret using specified subcommand
        usage: kubectl create secret [flags] [options]
        options: []
        subcommands:
          - name: docker-registry
            description: Create a secret for use with a Docker registry
            usage: kubectl create secret docker-registry NAME --docker-username=user --docker-password=password --docker-email=email [--docker-server=string] [--from-file=[key=]source] [--dry-run=server|client|none] [options]
            options:
              - names:
                  - --allow-missing-template-keys
                argument: "true"
                description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
              - names:
                  - --append-hash
                argument: "false"
                description: Append a hash of the secret to its name.
              - names:
                  - --docker-email
                argument: ''''''
                description: Email for Docker registry
              - names:
                  - --docker-password
                argument: ''''''
                description: Password for Docker registry authentication
              - names:
                  - --docker-server
                argument: '''https://index.docker.io/v1/'''
                description: Server location for Docker registry
              - names:
                  - --docker-username
                argument: ''''''
                description: Username for Docker registry authentication
              - names:
                  - --dry-run
                argument: '''none'''
                description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
              - names:
                  - --field-manager
                argument: '''kubectl-create'''
                description: Name of the manager used to track field ownership.
              - names:
                  - --from-file
                argument: '[]'
                description: Key files can be specified using their file path, in which case a default name will be given to them, or optionally with a name and file path, in which case the given name will be used. Specifying a directory will iterate each named file in the directory that is a valid secret key.
              - names:
                  - -o
                  - --output
                argument: ''''''
                description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
              - names:
                  - --save-config
                argument: "false"
                description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
              - names:
                  - --show-managed-fields
                argument: "false"
                description: If true, keep the managedFields when printing objects in JSON or YAML format.
              - names:
                  - --template
                argument: ''''''
                description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
              - names:
                  - --validate
                argument: '''strict'''
                description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
          - name: generic
            description: Create a secret from a local file, directory, or literal value
            usage: kubectl create secret generic NAME [--type=string] [--from-file=[key=]source] [--from-literal=key1=value1] [--dry-run=server|client|none] [options]
            options:
              - names:
                  - --allow-missing-template-keys
                argument: "true"
                description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
              - names:
                  - --append-hash
                argument: "false"
                description: Append a hash of the secret to its name.
              - names:
                  - --dry-run
                argument: '''none'''
                description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
              - names:
                  - --field-manager
                argument: '''kubectl-create'''
                description: Name of the manager used to track field ownership.
              - names:
                  - --from-env-file
                argument: '[]'
                description: Specify the path to a file to read lines of key=val pairs to create a secret.
              - names:
                  - --from-file
                argument: '[]'
                description: Key files can be specified using their file path, in which case a default name will be given to them, or optionally with a name and file path, in which case the given name will be used. Specifying a directory will iterate each named file in the directory that is a valid secret key.
              - names:
                  - --from-literal
                argument: '[]'
                description: Specify a key and literal value to insert in secret (i.e. mykey=somevalue)
              - names:
                  - -o
                  - --output
                argument: ''''''
                description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
              - names:
                  - --save-config
                argument: "false"
                description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
              - names:
                  - --show-managed-fields
                argument: "false"
                description: If true, keep the managedFields when printing objects in JSON or YAML format.
              - names:
                  - --template
                argument: ''''''
                description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
              - names:
                  - --type
                argument: ''''''
                description: The type of secret to create
              - names:
                  - --validate
                argument: '''strict'''
                description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
          - name: tls
            description: Create a TLS secret
            usage: kubectl create secret tls NAME --cert=path/to/cert/file --key=path/to/key/file [--dry-run=server|client|none] [options]
            options:
              - names:
                  - --allow-missing-template-keys
                argument: "true"
                description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
              - names:
                  - --append-hash
                argument: "false"
                description: Append a hash of the secret to its name.
              - names:
                  - --cert
                argument: ''''''
                description: Path to PEM encoded public key certificate.
              - names:
                  - --dry-run
                argument: '''none'''
                description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
              - names:
                  - --field-manager
                argument: '''kubectl-create'''
                description: Name of the manager used to track field ownership.
              - names:
                  - --key
                argument: ''''''
                description: Path to private key associated with given certificate.
              - names:
                  - -o
                  - --output
                argument: ''''''
                description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
              - names:
                  - --save-config
                argument: "false"
                description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
              - names:
                  - --show-managed-fields
                argument: "false"
                description: If true, keep the managedFields when printing objects in JSON or YAML format.
              - names:
                  - --template
                argument: ''''''
                description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
              - names:
                  - --validate
                argument: '''strict'''
                description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - name: service
        description: Create a service using a specified subcommand
        usage: kubectl create service [flags] [options]
        options: []
        subcommands:
          - name: clusterip
            description: Create a ClusterIP service
            usage: kubectl create service clusterip NAME [--tcp=<port>:<targetPort>] [--dry-run=server|client|none] [options]
            options:
              - names:
                  - --allow-missing-template-keys
                argument: "true"
                description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
              - names:
                  - --clusterip
                argument: ''''''
                description: Assign your own ClusterIP or set to 'None' for a 'headless' service (no loadbalancing).
              - names:
                  - --dry-run
                argument: '''none'''
                description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
              - names:
                  - --field-manager
                argument: '''kubectl-create'''
                description: Name of the manager used to track field ownership.
              - names:
                  - -o
                  - --output
                argument: ''''''
                description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
              - names:
                  - --save-config
                argument: "false"
                description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
              - names:
                  - --show-managed-fields
                argument: "false"
                description: If true, keep the managedFields when printing objects in JSON or YAML format.
              - names:
                  - --tcp
                argument: '[]'
                description: Port pairs can be specified as '<port>:<targetPort>'.
              - names:
                  - --template
                argument: ''''''
                description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
              - names:
                  - --validate
                argument: '''strict'''
                description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
          - name: externalname
            description: Create an ExternalName service
            usage: kubectl create service externalname NAME --external-name external.name [--dry-run=server|client|none] [options]
            options:
              - names:
                  - --allow-missing-template-keys
                argument: "true"
                description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
              - names:
                  - --dry-run
                argument: '''none'''
                description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
              - names:
                  - --external-name
                argument: ''''''
                description: External name of service
              - names:
                  - --field-manager
                argument: '''kubectl-create'''
                description: Name of the manager used to track field ownership.
              - names:
                  - -o
                  - --output
                argument: ''''''
                description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
              - names:
                  - --save-config
                argument: "false"
                description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
              - names:
                  - --show-managed-fields
                argument: "false"
                description: If true, keep the managedFields when printing objects in JSON or YAML format.
              - names:
                  - --tcp
                argument: '[]'
                description: Port pairs can be specified as '<port>:<targetPort>'.
              - names:
                  - --template
                argument: ''''''
                description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
              - names:
                  - --validate
                argument: '''strict'''
                description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
          - name: loadbalancer
            description: Create a LoadBalancer service
            usage: kubectl create service loadbalancer NAME [--tcp=port:targetPort] [--dry-run=server|client|none] [options]
            options:
              - names:
                  - --allow-missing-template-keys
                argument: "true"
                description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
              - names:
                  - --dry-run
                argument: '''none'''
                description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
              - names:
                  - --field-manager
                argument: '''kubectl-create'''
                description: Name of the manager used to track field ownership.
              - names:
                  - -o
                  - --output
                argument: ''''''
                description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
              - names:
                  - --save-config
                argument: "false"
                description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
              - names:
                  - --show-managed-fields
                argument: "false"
                description: If true, keep the managedFields when printing objects in JSON or YAML format.
              - names:
                  - --tcp
                argument: '[]'
                description: Port pairs can be specified as '<port>:<targetPort>'.
              - names:
                  - --template
                argument: ''''''
                description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
              - names:
                  - --validate
                argument: '''strict'''
                description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
          - name: nodeport
            description: Create a NodePort service
            usage: kubectl create service nodeport NAME [--tcp=port:targetPort] [--dry-run=server|client|none] [options]
            options:
              - names:
                  - --allow-missing-template-keys
                argument: "true"
                description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
              - names:
                  - --dry-run
                argument: '''none'''
                description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
              - names:
                  - --field-manager
                argument: '''kubectl-create'''
                description: Name of the manager used to track field ownership.
              - names:
                  - --node-port
                argument: "0"
                description: Port used to expose the service on each node in a cluster.
              - names:
                  - -o
                  - --output
                argument: ''''''
                description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
              - names:
                  - --save-config
                argument: "false"
                description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
              - names:
                  - --show-managed-fields
                argument: "false"
                description: If true, keep the managedFields when printing objects in JSON or YAML format.
              - names:
                  - --tcp
                argument: '[]'
                description: Port pairs can be specified as '<port>:<targetPort>'.
              - names:
                  - --template
                argument: ''''''
                description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
              - names:
                  - --validate
                argument: '''strict'''
                description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - name: serviceaccount
        description: Create a service account with the specified name
        usage: kubectl create serviceaccount NAME [--dry-run=server|client|none] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-create'''
            description: Name of the manager used to track field ownership.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --save-config
            argument: "false"
            description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --validate
            argument: '''strict'''
            description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - name: token
        description: Request a service account token
        usage: kubectl create token SERVICE_ACCOUNT_NAME [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --audience
            argument: '[]'
            description: Audience of the requested token. If unset, defaults to requesting a token for use with the Kubernetes API server. May be repeated to request a token valid for multiple audiences.
          - names:
              - --bound-object-kind
            argument: ''''''
            description: Kind of an object to bind the token to. Supported kinds are Pod, Secret. If set, --bound-object-name must be provided.
          - names:
              - --bound-object-name
            argument: ''''''
            description: Name of an object to bind the token to. The token will expire when the object is deleted. Requires --bound-object-kind.
          - names:
              - --bound-object-uid
            argument: ''''''
            description: UID of an object to bind the token to. Requires --bound-object-kind and --bound-object-name. If unset, the UID of the existing object is used.
          - names:
              - --duration
            argument: 0s
            description: Requested lifetime of the issued token. The server may return a token with a longer or shorter lifetime.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
  - name: expose
    description: Take a replication controller, service, deployment or pod and expose it as a new Kubernetes service
    usage: kubectl expose (-f FILENAME | TYPE NAME) [--port=port] [--protocol=TCP|UDP|SCTP] [--target-port=number-or-name] [--name=name] [--external-ip=external-ip-of-service] [--type=type] [options]
    options:
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --cluster-ip
        argument: ''''''
        description: ClusterIP to be assigned to the service. Leave empty to auto-allocate, or set to 'None' to create a headless service.
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - --external-ip
        argument: ''''''
        description: Additional external IP address (not managed by Kubernetes) to accept for the service. If this IP is routed to a node, the service can be accessed by this IP in addition to its generated service IP.
      - names:
          - --field-manager
        argument: '''kubectl-expose'''
        description: Name of the manager used to track field ownership.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: Filename, directory, or URL to files identifying the resource to expose a service
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process the kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - -l
          - --labels
        argument: ''''''
        description: Labels to apply to the service created by this call.
      - names:
          - --load-balancer-ip
        argument: ''''''
        description: IP to assign to the LoadBalancer. If empty, an ephemeral IP will be created and used (cloud-provider specific).
      - names:
          - --name
        argument: ''''''
        description: The name for the newly created object.
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
      - names:
          - --override-type
        argument: '''merge'''
        description: 'The method used to override the generated object: json, merge, or strategic.'
      - names:
          - --overrides
        argument: ''''''
        description: An inline JSON override for the generated object. If this is non-empty, it is used to override the generated object. Requires that the object supply a valid apiVersion field.
      - names:
          - --port
        argument: ''''''
        description: The port that the service should serve on. Copied from the resource being exposed, if unspecified
      - names:
          - --protocol
        argument: ''''''
        description: The network protocol for the service to be created. Default is 'TCP'.
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - --save-config
        argument: "false"
        description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
      - names:
          - --selector
        argument: ''''''
        description: A label selector to use for this service. Only equality-based selector requirements are supported. If empty (the default) infer the selector from the replication controller or replica set.)
      - names:
          - --session-affinity
        argument: ''''''
        description: 'If non-empty, set the session affinity for the service to this; legal values: ''None'', ''ClientIP'''
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - --target-port
        argument: ''''''
        description: Name or number for the port on the container that the service should direct traffic to. Optional.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - names:
          - --type
        argument: ''''''
        description: 'Type for this service: ClusterIP, NodePort, LoadBalancer, or ExternalName. Default is ''ClusterIP''.'
  - name: run
    description: Run a particular image on the cluster
    usage: kubectl run NAME --image=image [--env="key=value"] [--port=port] [--dry-run=server|client] [--overrides=inline-json] [--command] -- [COMMAND] [args...] [options]
    options:
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --annotations
        argument: '[]'
        description: Annotations to apply to the pod.
      - names:
          - --attach
        argument: "false"
        description: If true, wait for the Pod to start running, and then attach to the Pod as if 'kubectl attach ...' were called. Default false, unless '-i/--stdin' is set, in which case the default is true. With '--restart=Never' the exit code of the container process is returned.
      - names:
          - --cascade
        argument: '''background'''
        description: Must be "background", "orphan", or "foreground". Selects the deletion cascading strategy for the dependents (e.g. Pods created by a ReplicationController). Defaults to background.
      - names:
          - --command
        argument: "false"
        description: If true and extra arguments are present, use them as the 'command' field in the container, rather than the 'args' field which is the default.
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - --env
        argument: '[]'
        description: Environment variables to set in the container.
      - names:
          - --expose
        argument: "false"
        description: If true, create a ClusterIP service associated with the pod. Requires `--port`.
      - names:
          - --field-manager
        argument: '''kubectl-run'''
        description: Name of the manager used to track field ownership.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: to use to replace the resource.
      - names:
          - --force
        argument: "false"
        description: If true, immediately remove resources from API and bypass graceful deletion. Note that immediate deletion of some resources may result in inconsistency or data loss and requires confirmation.
      - names:
          - --grace-period
        argument: "-1"
        description: Period of time in seconds given to the resource to terminate gracefully. Ignored if negative. Set to 1 for immediate shutdown. Can only be set to 0 when --force is true (force deletion).
      - names:
          - --image
        argument: ''''''
        description: The image for the container to run.
      - names:
          - --image-pull-policy
        argument: ''''''
        description: The image pull policy for the container. If left empty, this value will not be specified by the client and defaulted by the server.
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process a kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - -l
          - --labels
        argument: ''''''
        description: Comma separated labels to apply to the pod. Will override previous values.
      - names:
          - --leave-stdin-open
        argument: "false"
        description: If the pod is started in interactive mode or with stdin, leave stdin open after the first attach completes. By default, stdin will be closed after the first attach completes.
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
      - names:
          - --override-type
        argument: '''merge'''
        description: 'The method used to override the generated object: json, merge, or strategic.'
      - names:
          - --overrides
        argument: ''''''
        description: An inline JSON override for the generated object. If this is non-empty, it is used to override the generated object. Requires that the object supply a valid apiVersion field.
      - names:
          - --pod-running-timeout
        argument: 1m0s
        description: The length of time (like 5s, 2m, or 3h, higher than zero) to wait until at least one pod is running
      - names:
          - --port
        argument: ''''''
        description: The port that this container exposes.
      - names:
          - --privileged
        argument: "false"
        description: If true, run the container in privileged mode.
      - names:
          - -q
          - --quiet
        argument: "false"
        description: If true, suppress prompt messages.
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - --restart
        argument: '''Always'''
        description: The restart policy for this Pod. Legal values [Always, OnFailure, Never].
      - names:
          - --rm
        argument: "false"
        description: If true, delete the pod after it exits. Only valid when attaching to the container, e.g. with '--attach' or with '-i/--stdin'.
      - names:
          - --save-config
        argument: "false"
        description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - -i
          - --stdin
        argument: "false"
        description: Keep stdin open on the container in the pod, even if nothing is attached.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - names:
          - --timeout
        argument: 0s
        description: The length of time to wait before giving up on a delete, zero means determine a timeout from the size of the object
      - names:
          - -t
          - --tty
        argument: "false"
        description: Allocate a TTY for the container in the pod.
      - names:
          - --wait
        argument: "false"
        description: If true, wait for resources to be gone before returning. This waits for finalizers.
  - name: set
    description: Set specific features on objects
    usage: kubectl set SUBCOMMAND [options]
    options: []
    subcommands:
      - name: env
        description: Update environment variables on a pod template
        usage: kubectl set env RESOURCE/NAME KEY_1=VAL_1 ... KEY_N=VAL_N [options]
        options:
          - names:
              - --all
            argument: "false"
            description: If true, select all resources in the namespace of the specified resource types
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - -c
              - --containers
            argument: '''*'''
            description: The names of containers in the selected pod templates to change - may use wildcards
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - -e
              - --env
            argument: '[]'
            description: Specify a key-value pair for an environment variable to set into each container.
          - names:
              - --field-manager
            argument: '''kubectl-set'''
            description: Name of the manager used to track field ownership.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files the resource to update the env
          - names:
              - --from
            argument: ''''''
            description: The name of a resource from which to inject environment variables
          - names:
              - --keys
            argument: '[]'
            description: Comma-separated list of keys to import from specified resource
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - --list
            argument: "false"
            description: If true, display the environment and any changes in the standard format. this flag will removed when we have kubectl view env.
          - names:
              - --local
            argument: "false"
            description: If true, set env will NOT contact api-server but run locally.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --overwrite
            argument: "true"
            description: If true, allow environment to be overwritten, otherwise reject updates that overwrite existing environment.
          - names:
              - --prefix
            argument: ''''''
            description: Prefix to append to variable names
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - --resolve
            argument: "false"
            description: If true, show secret or configmap references when listing variables
          - names:
              - -l
              - --selector
            argument: ''''''
            description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - name: image
        description: Update the image of a pod template
        usage: kubectl set image (-f FILENAME | TYPE NAME) CONTAINER_NAME_1=CONTAINER_IMAGE_1 ... CONTAINER_NAME_N=CONTAINER_IMAGE_N [options]
        options:
          - names:
              - --all
            argument: "false"
            description: Select all resources, in the namespace of the specified resource types
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-set'''
            description: Name of the manager used to track field ownership.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files identifying the resource to get from a server.
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - --local
            argument: "false"
            description: If true, set image will NOT contact api-server but run locally.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - -l
              - --selector
            argument: ''''''
            description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - name: resources
        description: Update resource requests/limits on objects with pod templates
        usage: kubectl set resources (-f FILENAME | TYPE NAME)  ([--limits=LIMITS & --requests=REQUESTS] [options]
        options:
          - names:
              - --all
            argument: "false"
            description: Select all resources, in the namespace of the specified resource types
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - -c
              - --containers
            argument: '''*'''
            description: The names of containers in the selected pod templates to change, all containers are selected by default - may use wildcards
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-set'''
            description: Name of the manager used to track field ownership.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files identifying the resource to get from a server.
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - --limits
            argument: ''''''
            description: The resource requirement requests for this container. For example, 'cpu=100m,memory=256Mi'. Note that server side components may assign requests depending on the server configuration, such as limit ranges.
          - names:
              - --local
            argument: "false"
            description: If true, set resources will NOT contact api-server but run locally.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - --requests
            argument: ''''''
            description: The resource requirement requests for this container. For example, 'cpu=100m,memory=256Mi'. Note that server side components may assign requests depending on the server configuration, such as limit ranges.
          - names:
              - -l
              - --selector
            argument: ''''''
            description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - name: selector
        description: Set the selector on a resource
        usage: kubectl set selector (-f FILENAME | TYPE NAME) EXPRESSIONS [--resource-version=version] [options]
        options:
          - names:
              - --all
            argument: "false"
            description: Select all resources in the namespace of the specified resource types
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-set'''
            description: Name of the manager used to track field ownership.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: identifying the resource.
          - names:
              - --local
            argument: "false"
            description: If true, annotation will NOT contact api-server but run locally.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - -R
              - --recursive
            argument: "true"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - --resource-version
            argument: ''''''
            description: If non-empty, the selectors update will only succeed if this is the current resource-version for the object. Only valid when specifying a single resource.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - name: serviceaccount
        description: Update the service account of a resource
        usage: kubectl set serviceaccount (-f FILENAME | TYPE NAME) SERVICE_ACCOUNT [options]
        options:
          - names:
              - --all
            argument: "false"
            description: Select all resources, in the namespace of the specified resource types
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-set'''
            description: Name of the manager used to track field ownership.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files identifying the resource to get from a server.
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - --local
            argument: "false"
            description: If true, set serviceaccount will NOT contact api-server but run locally.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - name: subject
        description: Update the user, group, or service account in a role binding or cluster role binding
        usage: kubectl set subject (-f FILENAME | TYPE NAME) [--user=username] [--group=groupname] [--serviceaccount=namespace:serviceaccountname] [--dry-run=server|client|none] [options]
        options:
          - names:
              - --all
            argument: "false"
            description: Select all resources, in the namespace of the specified resource types
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - --field-manager
            argument: '''kubectl-set'''
            description: Name of the manager used to track field ownership.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files the resource to update the subjects
          - names:
              - --group
            argument: '[]'
            description: Groups to bind to the role
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - --local
            argument: "false"
            description: If true, set subject will NOT contact api-server but run locally.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - -l
              - --selector
            argument: ''''''
            description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
          - names:
              - --serviceaccount
            argument: '[]'
            description: Service accounts to bind to the role
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
  - name: explain
    description: Get documentation for a resource
    usage: kubectl explain RESOURCE [options]
    options:
      - names:
          - --api-version
        argument: ''''''
        description: Get different explanations for particular API version (API group/version)
      - names:
          - --recursive
        argument: "false"
        description: Print the fields of fields (Currently only 1 level deep)
  - name: get
    description: Display one or many resources
    usage: kubectl get [(-o|--output=)json|yaml|name|go-template|go-template-file|template|templatefile|jsonpath|jsonpath-as-json|jsonpath-file|custom-columns|custom-columns-file|wide] (TYPE[.VERSION][.GROUP] [NAME | -l label] | TYPE[.VERSION][.GROUP]/NAME ...) [flags] [options]
    options:
      - names:
          - -A
          - --all-namespaces
        argument: "false"
        description: If present, list the requested object(s) across all namespaces. Namespace in current context is ignored even if specified with --namespace.
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --chunk-size
        argument: "500"
        description: Return large lists in chunks rather than all at once. Pass 0 to disable. This flag is beta and may change in the future.
      - names:
          - --field-selector
        argument: ''''''
        description: Selector (field query) to filter on, supports '=', '==', and '!='.(e.g. --field-selector key1=value1,key2=value2). The server only supports a limited number of field queries per type.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: Filename, directory, or URL to files identifying the resource to get from a server.
      - names:
          - --ignore-not-found
        argument: "false"
        description: If the requested object does not exist the command will return exit code 0.
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process the kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - -L
          - --label-columns
        argument: '[]'
        description: Accepts a comma separated list of labels that are going to be presented as columns. Names are case-sensitive. You can also use multiple flag options like -L label1 -L label2...
      - names:
          - --no-headers
        argument: "false"
        description: When using the default or custom-column output format, don't print headers (default print headers).
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file, custom-columns, custom-columns-file, wide). See custom columns [https://kubernetes.io/docs/reference/kubectl/#custom-columns], golang template [http://golang.org/pkg/text/template/#pkg-overview] and jsonpath template [https://kubernetes.io/docs/reference/kubectl/jsonpath/].'
      - names:
          - --output-watch-events
        argument: "false"
        description: Output watch event objects when --watch or --watch-only is used. Existing objects are output as initial ADDED events.
      - names:
          - --raw
        argument: ''''''
        description: Raw URI to request from the server. Uses the transport specified by the kubeconfig file.
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
      - names:
          - --server-print
        argument: "true"
        description: If true, have the server return the appropriate table output. Supports extension APIs and CRDs.
      - names:
          - --show-kind
        argument: "false"
        description: If present, list the resource type for the requested object(s).
      - names:
          - --show-labels
        argument: "false"
        description: When printing, show all labels as the last column (default hide labels column)
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - --sort-by
        argument: ''''''
        description: If non-empty, sort list types using this field specification. The field specification is expressed as a JSONPath expression (e.g. '{.metadata.name}'). The field in the API resource specified by this JSONPath expression must be an integer or a string.
      - names:
          - --subresource
        argument: ''''''
        description: If specified, gets the subresource of the requested object. Must be one of [status scale]. This flag is alpha and may change in the future.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - names:
          - -w
          - --watch
        argument: "false"
        description: After listing/getting the requested object, watch for changes.
      - names:
          - --watch-only
        argument: "false"
        description: Watch for changes to the requested object(s), without listing/getting first.
  - name: edit
    description: Edit a resource on the server
    usage: kubectl edit (RESOURCE/NAME | -f FILENAME) [options]
    options:
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --field-manager
        argument: '''kubectl-edit'''
        description: Name of the manager used to track field ownership.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: Filename, directory, or URL to files to use to edit the resource
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process the kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
      - names:
          - --output-patch
        argument: "false"
        description: Output the patch if the resource is edited.
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - --save-config
        argument: "false"
        description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - --subresource
        argument: ''''''
        description: If specified, edit will operate on the subresource of the requested object. Must be one of [status]. This flag is alpha and may change in the future.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - names:
          - --validate
        argument: '''strict'''
        description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - names:
          - --windows-line-endings
        argument: "false"
        description: Defaults to the line ending native to your platform.
  - name: delete
    description: Delete resources by file names, stdin, resources and names, or by resources and label selector
    usage: kubectl delete ([-f FILENAME] | [-k DIRECTORY] | TYPE [(NAME | -l label | --all)]) [options]
    options:
      - names:
          - --all
        argument: "false"
        description: Delete all resources, in the namespace of the specified resource types.
      - names:
          - -A
          - --all-namespaces
        argument: "false"
        description: If present, list the requested object(s) across all namespaces. Namespace in current context is ignored even if specified with --namespace.
      - names:
          - --cascade
        argument: '''background'''
        description: Must be "background", "orphan", or "foreground". Selects the deletion cascading strategy for the dependents (e.g. Pods created by a ReplicationController). Defaults to background.
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - --field-selector
        argument: ''''''
        description: Selector (field query) to filter on, supports '=', '==', and '!='.(e.g. --field-selector key1=value1,key2=value2). The server only supports a limited number of field queries per type.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: containing the resource to delete.
      - names:
          - --force
        argument: "false"
        description: If true, immediately remove resources from API and bypass graceful deletion. Note that immediate deletion of some resources may result in inconsistency or data loss and requires confirmation.
      - names:
          - --grace-period
        argument: "-1"
        description: Period of time in seconds given to the resource to terminate gracefully. Ignored if negative. Set to 1 for immediate shutdown. Can only be set to 0 when --force is true (force deletion).
      - names:
          - --ignore-not-found
        argument: "false"
        description: Treat "resource not found" as a successful delete. Defaults to "true" when --all is specified.
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process a kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - --now
        argument: "false"
        description: If true, resources are signaled for immediate shutdown (same as --grace-period=1).
      - names:
          - -o
          - --output
        argument: ''''''
        description: Output mode. Use "-o name" for shorter output (resource/name).
      - names:
          - --raw
        argument: ''''''
        description: Raw URI to DELETE to the server. Uses the transport specified by the kubeconfig file.
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
      - names:
          - --timeout
        argument: 0s
        description: The length of time to wait before giving up on a delete, zero means determine a timeout from the size of the object
      - names:
          - --wait
        argument: "true"
        description: If true, wait for resources to be gone before returning. This waits for finalizers.
  - name: rollout
    description: Manage the rollout of a resource
    usage: kubectl rollout SUBCOMMAND [options]
    options: []
    subcommands:
      - name: history
        description: View rollout history
        usage: kubectl rollout history (TYPE NAME | TYPE/NAME) [flags] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files identifying the resource to get from a server.
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - --revision
            argument: "0"
            description: See the details, including podTemplate of the revision specified
          - names:
              - -l
              - --selector
            argument: ''''''
            description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - name: pause
        description: Mark the provided resource as paused
        usage: kubectl rollout pause RESOURCE [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --field-manager
            argument: '''kubectl-rollout'''
            description: Name of the manager used to track field ownership.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files identifying the resource to get from a server.
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - -l
              - --selector
            argument: ''''''
            description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - name: restart
        description: Restart a resource
        usage: kubectl rollout restart RESOURCE [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --field-manager
            argument: '''kubectl-rollout'''
            description: Name of the manager used to track field ownership.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files identifying the resource to get from a server.
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - -l
              - --selector
            argument: ''''''
            description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - name: resume
        description: Resume a paused resource
        usage: kubectl rollout resume RESOURCE [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --field-manager
            argument: '''kubectl-rollout'''
            description: Name of the manager used to track field ownership.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files identifying the resource to get from a server.
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - -l
              - --selector
            argument: ''''''
            description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - name: status
        description: Show the status of the rollout
        usage: kubectl rollout status (TYPE NAME | TYPE/NAME) [flags] [options]
        options:
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files identifying the resource to get from a server.
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - --revision
            argument: "0"
            description: Pin to a specific revision for showing its status. Defaults to 0 (last revision).
          - names:
              - -l
              - --selector
            argument: ''''''
            description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
          - names:
              - --timeout
            argument: 0s
            description: The length of time to wait before ending watch, zero means never. Any other values should contain a corresponding time unit (e.g. 1s, 2m, 3h).
          - names:
              - -w
              - --watch
            argument: "true"
            description: Watch the status of the rollout until it's done.
      - name: undo
        description: Undo a previous rollout
        usage: kubectl rollout undo (TYPE NAME | TYPE/NAME) [flags] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files identifying the resource to get from a server.
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - -l
              - --selector
            argument: ''''''
            description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --to-revision
            argument: "0"
            description: The revision to rollback to. Default to 0 (last revision).
  - name: scale
    description: Set a new size for a deployment, replica set, or replication controller
    usage: kubectl scale [--resource-version=version] [--current-replicas=count] --replicas=COUNT (-f FILENAME | TYPE NAME) [options]
    options:
      - names:
          - --all
        argument: "false"
        description: Select all resources in the namespace of the specified resource types
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --current-replicas
        argument: "-1"
        description: Precondition for current size. Requires that the current size of the resource match this value in order to scale. -1 (default) for no condition.
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: Filename, directory, or URL to files identifying the resource to set a new size
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process the kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - --replicas
        argument: "0"
        description: The new desired number of replicas. Required.
      - names:
          - --resource-version
        argument: ''''''
        description: Precondition for resource version. Requires that the current resource version match this value in order to scale.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - names:
          - --timeout
        argument: 0s
        description: The length of time to wait before giving up on a scale operation, zero means don't wait. Any other values should contain a corresponding time unit (e.g. 1s, 2m, 3h).
  - name: autoscale
    description: Auto-scale a deployment, replica set, stateful set, or replication controller
    usage: kubectl autoscale (-f FILENAME | TYPE NAME | TYPE/NAME) [--min=MINPODS] --max=MAXPODS [--cpu-percent=CPU] [options]
    options:
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --cpu-percent
        argument: "-1"
        description: The target average CPU utilization (represented as a percent of requested CPU) over all the pods. If it's not specified or negative, a default autoscaling policy will be used.
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - --field-manager
        argument: '''kubectl-autoscale'''
        description: Name of the manager used to track field ownership.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: Filename, directory, or URL to files identifying the resource to autoscale.
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process the kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - --max
        argument: "-1"
        description: The upper limit for the number of pods that can be set by the autoscaler. Required.
      - names:
          - --min
        argument: "-1"
        description: The lower limit for the number of pods that can be set by the autoscaler. If it's not specified or negative, the server will apply a default value.
      - names:
          - --name
        argument: ''''''
        description: The name for the newly created object. If not specified, the name of the input resource will be used.
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - --save-config
        argument: "false"
        description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
  - name: certificate
    description: Modify certificate resources.
    usage: kubectl certificate SUBCOMMAND [options]
    options: []
    subcommands:
      - name: approve
        description: Approve a certificate signing request
        usage: kubectl certificate approve (-f FILENAME | NAME) [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files identifying the resource to update
          - names:
              - --force
            argument: "false"
            description: Update the CSR even if it is already approved.
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - name: deny
        description: Deny a certificate signing request
        usage: kubectl certificate deny (-f FILENAME | NAME) [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files identifying the resource to update
          - names:
              - --force
            argument: "false"
            description: Update the CSR even if it is already denied.
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
  - name: cluster-info
    description: Display cluster information
    usage: kubectl cluster-info [flags] [options]
    options: []
  - name: top
    description: Display resource (CPU/memory) usage
    usage: kubectl top [flags] [options]
    options: []
    subcommands:
      - name: node
        description: Display resource (CPU/memory) usage of nodes
        usage: kubectl top node [NAME | -l label] [options]
        options:
          - names:
              - --no-headers
            argument: "false"
            description: If present, print output without headers
          - names:
              - -l
              - --selector
            argument: ''''''
            description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
          - names:
              - --show-capacity
            argument: "false"
            description: Print node resources based on Capacity instead of Allocatable(default) of the nodes.
          - names:
              - --sort-by
            argument: ''''''
            description: If non-empty, sort nodes list using specified field. The field can be either 'cpu' or 'memory'.
          - names:
              - --use-protocol-buffers
            argument: "true"
            description: Enables using protocol-buffers to access Metrics API.
      - name: pod
        description: Display resource (CPU/memory) usage of pods
        usage: kubectl top pod [NAME | -l label] [options]
        options:
          - names:
              - -A
              - --all-namespaces
            argument: "false"
            description: If present, list the requested object(s) across all namespaces. Namespace in current context is ignored even if specified with --namespace.
          - names:
              - --containers
            argument: "false"
            description: If present, print usage of containers within a pod.
          - names:
              - --field-selector
            argument: ''''''
            description: Selector (field query) to filter on, supports '=', '==', and '!='.(e.g. --field-selector key1=value1,key2=value2). The server only supports a limited number of field queries per type.
          - names:
              - --no-headers
            argument: "false"
            description: If present, print output without headers.
          - names:
              - -l
              - --selector
            argument: ''''''
            description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
          - names:
              - --sort-by
            argument: ''''''
            description: If non-empty, sort pods list using specified field. The field can be either 'cpu' or 'memory'.
          - names:
              - --sum
            argument: "false"
            description: Print the sum of the resource usage
          - names:
              - --use-protocol-buffers
            argument: "true"
            description: Enables using protocol-buffers to access Metrics API.
  - name: cordon
    description: Mark node as unschedulable
    usage: kubectl cordon NODE [options]
    options:
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
  - name: uncordon
    description: Mark node as schedulable
    usage: kubectl uncordon NODE [options]
    options:
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
  - name: drain
    description: Drain node in preparation for maintenance
    usage: kubectl drain NODE [options]
    options:
      - names:
          - --chunk-size
        argument: "500"
        description: Return large lists in chunks rather than all at once. Pass 0 to disable. This flag is beta and may change in the future.
      - names:
          - --delete-emptydir-data
        argument: "false"
        description: Continue even if there are pods using emptyDir (local data that will be deleted when the node is drained).
      - names:
          - --disable-eviction
        argument: "false"
        description: Force drain to use delete, even if eviction is supported. This will bypass checking PodDisruptionBudgets, use with caution.
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - --force
        argument: "false"
        description: Continue even if there are pods that do not declare a controller.
      - names:
          - --grace-period
        argument: "-1"
        description: Period of time in seconds given to each pod to terminate gracefully. If negative, the default value specified in the pod will be used.
      - names:
          - --ignore-daemonsets
        argument: "false"
        description: Ignore DaemonSet-managed pods.
      - names:
          - --pod-selector
        argument: ''''''
        description: Label selector to filter pods on the node
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
      - names:
          - --skip-wait-for-delete-timeout
        argument: "0"
        description: If pod DeletionTimestamp older than N seconds, skip waiting for the pod. Seconds must be greater than 0 to skip.
      - names:
          - --timeout
        argument: 0s
        description: The length of time to wait before giving up, zero means infinite
  - name: taint
    description: Update the taints on one or more nodes
    usage: kubectl taint NODE NAME KEY_1=VAL_1:TAINT_EFFECT_1 ... KEY_N=VAL_N:TAINT_EFFECT_N [options]
    options:
      - names:
          - --all
        argument: "false"
        description: Select all nodes in the cluster
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - --field-manager
        argument: '''kubectl-taint'''
        description: Name of the manager used to track field ownership.
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
      - names:
          - --overwrite
        argument: "false"
        description: If true, allow taints to be overwritten, otherwise reject taint updates that overwrite existing taints.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - names:
          - --validate
        argument: '''strict'''
        description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
  - name: describe
    description: Show details of a specific resource or group of resources
    usage: kubectl describe (-f FILENAME | TYPE [NAME_PREFIX | -l label] | TYPE/NAME) [options]
    options:
      - names:
          - -A
          - --all-namespaces
        argument: "false"
        description: If present, list the requested object(s) across all namespaces. Namespace in current context is ignored even if specified with --namespace.
      - names:
          - --chunk-size
        argument: "500"
        description: Return large lists in chunks rather than all at once. Pass 0 to disable. This flag is beta and may change in the future.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: Filename, directory, or URL to files containing the resource to describe
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process the kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
      - names:
          - --show-events
        argument: "true"
        description: If true, display events related to the described object.
  - name: logs
    description: Print the logs for a container in a pod
    usage: kubectl logs [-f] [-p] (POD | TYPE/NAME) [-c CONTAINER] [options]
    options:
      - names:
          - --all-containers
        argument: "false"
        description: Get all containers' logs in the pod(s).
      - names:
          - -c
          - --container
        argument: ''''''
        description: Print the logs of this container
      - names:
          - -f
          - --follow
        argument: "false"
        description: Specify if the logs should be streamed.
      - names:
          - --ignore-errors
        argument: "false"
        description: If watching / following pod logs, allow for any errors that occur to be non-fatal
      - names:
          - --insecure-skip-tls-verify-backend
        argument: "false"
        description: Skip verifying the identity of the kubelet that logs are requested from. In theory, an attacker could provide invalid log content back. You might want to use this if your kubelet serving certificates have expired.
      - names:
          - --limit-bytes
        argument: "0"
        description: Maximum bytes of logs to return. Defaults to no limit.
      - names:
          - --max-log-requests
        argument: "5"
        description: Specify maximum number of concurrent logs to follow when using by a selector. Defaults to 5.
      - names:
          - --pod-running-timeout
        argument: 20s
        description: The length of time (like 5s, 2m, or 3h, higher than zero) to wait until at least one pod is running
      - names:
          - --prefix
        argument: "false"
        description: Prefix each log line with the log source (pod name and container name)
      - names:
          - -p
          - --previous
        argument: "false"
        description: If true, print the logs for the previous instance of the container in a pod if it exists.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
      - names:
          - --since
        argument: 0s
        description: Only return logs newer than a relative duration like 5s, 2m, or 3h. Defaults to all logs. Only one of since-time / since may be used.
      - names:
          - --since-time
        argument: ''''''
        description: Only return logs after a specific date (RFC3339). Defaults to all logs. Only one of since-time / since may be used.
      - names:
          - --tail
        argument: "-1"
        description: Lines of recent log file to display. Defaults to -1 with no selector, showing all log lines otherwise 10, if a selector is provided.
      - names:
          - --timestamps
        argument: "false"
        description: Include timestamps on each line in the log output
  - name: attach
    description: Attach to a running container
    usage: kubectl attach (POD | TYPE/NAME) -c CONTAINER [options]
    options:
      - names:
          - -c
          - --container
        argument: ''''''
        description: Container name. If omitted, use the kubectl.kubernetes.io/default-container annotation for selecting the container to be attached or the first container in the pod will be chosen
      - names:
          - --pod-running-timeout
        argument: 1m0s
        description: The length of time (like 5s, 2m, or 3h, higher than zero) to wait until at least one pod is running
      - names:
          - -q
          - --quiet
        argument: "false"
        description: Only print output from the remote session
      - names:
          - -i
          - --stdin
        argument: "false"
        description: Pass stdin to the container
      - names:
          - -t
          - --tty
        argument: "false"
        description: Stdin is a TTY
  - name: exec
    description: Execute a command in a container
    usage: kubectl exec (POD | TYPE/NAME) [-c CONTAINER] [flags] -- COMMAND [args...] [options]
    options:
      - names:
          - -c
          - --container
        argument: ''''''
        description: Container name. If omitted, use the kubectl.kubernetes.io/default-container annotation for selecting the container to be attached or the first container in the pod will be chosen
      - names:
          - -f
          - --filename
        argument: '[]'
        description: to use to exec into the resource
      - names:
          - --pod-running-timeout
        argument: 1m0s
        description: The length of time (like 5s, 2m, or 3h, higher than zero) to wait until at least one pod is running
      - names:
          - -q
          - --quiet
        argument: "false"
        description: Only print output from the remote session
      - names:
          - -i
          - --stdin
        argument: "false"
        description: Pass stdin to the container
      - names:
          - -t
          - --tty
        argument: "false"
        description: Stdin is a TTY
  - name: port-forward
    description: Forward one or more local ports to a pod
    usage: kubectl port-forward TYPE/NAME [options] [LOCAL_PORT:]REMOTE_PORT [...[LOCAL_PORT_N:]REMOTE_PORT_N]
    options:
      - names:
          - --address
        argument: '[localhost]'
        description: Addresses to listen on (comma separated). Only accepts IP addresses or localhost as a value. When localhost is supplied, kubectl will try to bind on both 127.0.0.1 and ::1 and will fail if neither of these addresses are available to bind.
      - names:
          - --pod-running-timeout
        argument: 1m0s
        description: The length of time (like 5s, 2m, or 3h, higher than zero) to wait until at least one pod is running
  - name: proxy
    description: Run a proxy to the Kubernetes API server
    usage: kubectl proxy [--port=PORT] [--www=static-dir] [--www-prefix=prefix] [--api-prefix=prefix] [options]
    options:
      - names:
          - --accept-hosts
        argument: '''^localhost$,^127\.0\.0\.1$,^\[::1\]$'''
        description: Regular expression for hosts that the proxy should accept.
      - names:
          - --accept-paths
        argument: '''^.*'''
        description: Regular expression for paths that the proxy should accept.
      - names:
          - --address
        argument: '''127.0.0.1'''
        description: The IP address on which to serve on.
      - names:
          - --api-prefix
        argument: '''/'''
        description: Prefix to serve the proxied API under.
      - names:
          - --append-server-path
        argument: "false"
        description: If true, enables automatic path appending of the kube context server path to each request.
      - names:
          - --disable-filter
        argument: "false"
        description: If true, disable request filtering in the proxy. This is dangerous, and can leave you vulnerable to XSRF attacks, when used with an accessible port.
      - names:
          - --keepalive
        argument: 0s
        description: keepalive specifies the keep-alive period for an active network connection. Set to 0 to disable keepalive.
      - names:
          - -p
          - --port
        argument: "8001"
        description: The port on which to run the proxy. Set to 0 to pick a random port.
      - names:
          - --reject-methods
        argument: '''^$'''
        description: Regular expression for HTTP methods that the proxy should reject (example --reject-methods='POST,PUT,PATCH').
      - names:
          - --reject-paths
        argument: '''^/api/.*/pods/.*/exec,^/api/.*/pods/.*/attach'''
        description: Regular expression for paths that the proxy should reject. Paths specified here will be rejected even accepted by --accept-paths.
      - names:
          - -u
          - --unix-socket
        argument: ''''''
        description: Unix socket on which to run the proxy.
      - names:
          - -w
          - --www
        argument: ''''''
        description: Also serve static files from the given directory under the specified prefix.
      - names:
          - -P
          - --www-prefix
        argument: '''/static/'''
        description: Prefix to serve static files under, if static file directory is specified.
  - name: cp
    description: Copy files and directories to and from containers
    usage: kubectl cp <file-spec-src> <file-spec-dest> [options]
    options:
      - names:
          - -c
          - --container
        argument: ''''''
        description: Container name. If omitted, use the kubectl.kubernetes.io/default-container annotation for selecting the container to be attached or the first container in the pod will be chosen
      - names:
          - --no-preserve
        argument: "false"
        description: The copied file/directory's ownership and permissions will not be preserved in the container
      - names:
          - --retries
        argument: "0"
        description: Set number of retries to complete a copy operation from a container. Specify 0 to disable or any negative value for infinite retrying. The default is 0 (no retry).
  - name: auth
    description: Inspect authorization
    usage: kubectl auth [flags] [options]
    options: []
    subcommands:
      - name: can-i
        description: Check whether an action is allowed
        usage: kubectl auth can-i VERB [TYPE | TYPE/NAME | NONRESOURCEURL] [options]
        options:
          - names:
              - -A
              - --all-namespaces
            argument: "false"
            description: If true, check the specified action in all namespaces.
          - names:
              - --list
            argument: "false"
            description: If true, prints all allowed actions.
          - names:
              - --no-headers
            argument: "false"
            description: If true, prints allowed actions without headers
          - names:
              - -q
              - --quiet
            argument: "false"
            description: If true, suppress output and just return the exit code.
          - names:
              - --subresource
            argument: ''''''
            description: SubResource such as pod/log or deployment/scale
      - name: reconcile
        description: Reconciles rules for RBAC role, role binding, cluster role, and cluster role binding objects
        usage: kubectl auth reconcile -f FILENAME [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --dry-run
            argument: '''none'''
            description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
          - names:
              - -f
              - --filename
            argument: '[]'
            description: Filename, directory, or URL to files identifying the resource to reconcile.
          - names:
              - -k
              - --kustomize
            argument: ''''''
            description: Process the kustomization directory. This flag can't be used together with -f or -R.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - -R
              - --recursive
            argument: "false"
            description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
          - names:
              - --remove-extra-permissions
            argument: "false"
            description: If true, removes extra permissions added to roles
          - names:
              - --remove-extra-subjects
            argument: "false"
            description: If true, removes extra subjects added to rolebindings
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
  - name: debug
    description: Create debugging sessions for troubleshooting workloads and nodes
    usage: kubectl debug (POD | TYPE[[.VERSION].GROUP]/NAME) [ -- COMMAND [args...] ] [options]
    options:
      - names:
          - --arguments-only
        argument: "false"
        description: If specified, everything after -- will be passed to the new container as Args instead of Command.
      - names:
          - --attach
        argument: "false"
        description: If true, wait for the container to start running, and then attach as if 'kubectl attach ...' were called. Default false, unless '-i/--stdin' is set, in which case the default is true.
      - names:
          - -c
          - --container
        argument: ''''''
        description: Container name to use for debug container.
      - names:
          - --copy-to
        argument: ''''''
        description: Create a copy of the target Pod with this name.
      - names:
          - --env
        argument: '[]'
        description: Environment variables to set in the container.
      - names:
          - --image
        argument: ''''''
        description: Container image to use for debug container.
      - names:
          - --image-pull-policy
        argument: ''''''
        description: The image pull policy for the container. If left empty, this value will not be specified by the client and defaulted by the server.
      - names:
          - -q
          - --quiet
        argument: "false"
        description: If true, suppress informational messages.
      - names:
          - --replace
        argument: "false"
        description: When used with '--copy-to', delete the original Pod.
      - names:
          - --same-node
        argument: "false"
        description: When used with '--copy-to', schedule the copy of target Pod on the same node.
      - names:
          - --set-image
        argument: '[]'
        description: When used with '--copy-to', a list of name=image pairs for changing container images, similar to how 'kubectl set image' works.
      - names:
          - --share-processes
        argument: "true"
        description: When used with '--copy-to', enable process namespace sharing in the copy.
      - names:
          - -i
          - --stdin
        argument: "false"
        description: Keep stdin open on the container(s) in the pod, even if nothing is attached.
      - names:
          - --target
        argument: ''''''
        description: When using an ephemeral container, target processes in this container name.
      - names:
          - -t
          - --tty
        argument: "false"
        description: Allocate a TTY for the debugging container.
  - name: diff
    description: Diff the live version against a would-be applied version
    usage: kubectl diff -f FILENAME [options]
    options:
      - names:
          - --field-manager
        argument: '''kubectl-client-side-apply'''
        description: Name of the manager used to track field ownership.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: Filename, directory, or URL to files contains the configuration to diff
      - names:
          - --force-conflicts
        argument: "false"
        description: If true, server-side apply will force the changes against conflicts.
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process the kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - --prune
        argument: "false"
        description: Include resources that would be deleted by pruning. Can be used with -l and default shows all resources would be pruned
      - names:
          - --prune-allowlist
        argument: '[]'
        description: Overwrite the default whitelist with <group/version/kind> for --prune
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
      - names:
          - --server-side
        argument: "false"
        description: If true, apply runs in the server instead of the client.
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, include managed fields in the diff.
  - name: apply
    description: Apply a configuration to a resource by file name or stdin
    usage: kubectl apply (-f FILENAME | -k DIRECTORY) [options]
    options:
      - names:
          - --all
        argument: "false"
        description: Select all resources in the namespace of the specified resource types.
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --cascade
        argument: '''background'''
        description: Must be "background", "orphan", or "foreground". Selects the deletion cascading strategy for the dependents (e.g. Pods created by a ReplicationController). Defaults to background.
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - --field-manager
        argument: '''kubectl-client-side-apply'''
        description: Name of the manager used to track field ownership.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: The files that contain the configurations to apply.
      - names:
          - --force
        argument: "false"
        description: If true, immediately remove resources from API and bypass graceful deletion. Note that immediate deletion of some resources may result in inconsistency or data loss and requires confirmation.
      - names:
          - --force-conflicts
        argument: "false"
        description: If true, server-side apply will force the changes against conflicts.
      - names:
          - --grace-period
        argument: "-1"
        description: Period of time in seconds given to the resource to terminate gracefully. Ignored if negative. Set to 1 for immediate shutdown. Can only be set to 0 when --force is true (force deletion).
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process a kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - --openapi-patch
        argument: "true"
        description: If true, use openapi to calculate diff when the openapi presents and the resource can be found in the openapi spec. Otherwise, fall back to use baked-in types.
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
      - names:
          - --overwrite
        argument: "true"
        description: Automatically resolve conflicts between the modified and live configuration by using values from the modified configuration
      - names:
          - --prune
        argument: "false"
        description: Automatically delete resource objects, that do not appear in the configs and are created by either apply or create --save-config. Should be used with either -l or --all.
      - names:
          - --prune-whitelist
        argument: '[]'
        description: Overwrite the default whitelist with <group/version/kind> for --prune
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
      - names:
          - --server-side
        argument: "false"
        description: If true, apply runs in the server instead of the client.
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - names:
          - --timeout
        argument: 0s
        description: The length of time to wait before giving up on a delete, zero means determine a timeout from the size of the object
      - names:
          - --validate
        argument: '''strict'''
        description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - names:
          - --wait
        argument: "false"
        description: If true, wait for resources to be gone before returning. This waits for finalizers.
  - name: patch
    description: Update fields of a resource
    usage: kubectl patch (-f FILENAME | TYPE NAME) [-p PATCH|--patch-file FILE] [options]
    options:
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - --field-manager
        argument: '''kubectl-patch'''
        description: Name of the manager used to track field ownership.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: Filename, directory, or URL to files identifying the resource to update
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process the kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - --local
        argument: "false"
        description: If true, patch will operate on the content of the file, not the server-side resource.
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
      - names:
          - -p
          - --patch
        argument: ''''''
        description: The patch to be applied to the resource JSON file.
      - names:
          - --patch-file
        argument: ''''''
        description: A file containing a patch to be applied to the resource.
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - --subresource
        argument: ''''''
        description: If specified, patch will operate on the subresource of the requested object. Must be one of [status scale]. This flag is alpha and may change in the future.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - names:
          - --type
        argument: '''strategic'''
        description: The type of patch being provided; one of [json merge strategic]
  - name: replace
    description: Replace a resource by file name or stdin
    usage: kubectl replace -f FILENAME [options]
    options:
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --cascade
        argument: '''background'''
        description: Must be "background", "orphan", or "foreground". Selects the deletion cascading strategy for the dependents (e.g. Pods created by a ReplicationController). Defaults to background.
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - --field-manager
        argument: '''kubectl-replace'''
        description: Name of the manager used to track field ownership.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: The files that contain the configurations to replace.
      - names:
          - --force
        argument: "false"
        description: If true, immediately remove resources from API and bypass graceful deletion. Note that immediate deletion of some resources may result in inconsistency or data loss and requires confirmation.
      - names:
          - --grace-period
        argument: "-1"
        description: Period of time in seconds given to the resource to terminate gracefully. Ignored if negative. Set to 1 for immediate shutdown. Can only be set to 0 when --force is true (force deletion).
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process a kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
      - names:
          - --raw
        argument: ''''''
        description: Raw URI to PUT to the server. Uses the transport specified by the kubeconfig file.
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - --save-config
        argument: "false"
        description: If true, the configuration of current object will be saved in its annotation. Otherwise, the annotation will be unchanged. This flag is useful when you want to perform kubectl apply on this object in the future.
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - --subresource
        argument: ''''''
        description: If specified, replace will operate on the subresource of the requested object. Must be one of [status scale]. This flag is alpha and may change in the future.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - names:
          - --timeout
        argument: 0s
        description: The length of time to wait before giving up on a delete, zero means determine a timeout from the size of the object
      - names:
          - --validate
        argument: '''strict'''
        description: 'Must be one of: strict (or true), warn, ignore (or false). "true" or "strict" will use a schema to validate the input and fail the request if invalid. It will perform server side validation if ServerSideFieldValidation is enabled on the api-server, but will fall back to less reliable client-side validation if not. "warn" will warn about unknown or duplicate fields without blocking the request if server-side field validation is enabled on the API server, and behave as "ignore" otherwise. "false" or "ignore" will not perform any schema validation, silently dropping any unknown or duplicate fields.'
      - names:
          - --wait
        argument: "false"
        description: If true, wait for resources to be gone before returning. This waits for finalizers.
  - name: wait
    description: 'Experimental: Wait for a specific condition on one or many resources'
    usage: kubectl wait ([-f FILENAME] | resource.group/resource.name | resource.group [(-l label | --all)]) [--for=delete|--for condition=available|--for=jsonpath='{}'=value] [options]
    options:
      - names:
          - --all
        argument: "false"
        description: Select all resources in the namespace of the specified resource types
      - names:
          - -A
          - --all-namespaces
        argument: "false"
        description: If present, list the requested object(s) across all namespaces. Namespace in current context is ignored even if specified with --namespace.
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --field-selector
        argument: ''''''
        description: Selector (field query) to filter on, supports '=', '==', and '!='.(e.g. --field-selector key1=value1,key2=value2). The server only supports a limited number of field queries per type.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: identifying the resource.
      - names:
          - --for
        argument: ''''''
        description: 'The condition to wait on: [delete|condition=condition-name[=condition-value]|jsonpath=''{JSONPath expression}''=JSONPath Condition]. The default condition-value is true. Condition values are compared after Unicode simple case folding, which is a more general form of case-insensitivity.'
      - names:
          - --local
        argument: "false"
        description: If true, annotation will NOT contact api-server but run locally.
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
      - names:
          - -R
          - --recursive
        argument: "true"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2)
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
      - names:
          - --timeout
        argument: 30s
        description: The length of time to wait before giving up. Zero means check once and don't wait, negative means wait for a week.
  - name: kustomize
    description: Build a kustomization target from a directory or URL.
    usage: kubectl kustomize DIR [flags] [options]
    options:
      - names:
          - --as-current-user
        argument: "false"
        description: use the uid and gid of the command executor to run the function in the container
      - names:
          - --enable-alpha-plugins
        argument: "false"
        description: enable kustomize plugins
      - names:
          - --enable-helm
        argument: "false"
        description: Enable use of the Helm chart inflator generator.
      - names:
          - -e
          - --env
        argument: '[]'
        description: a list of environment variables to be used by functions
      - names:
          - --helm-command
        argument: '''helm'''
        description: helm command (path to executable)
      - names:
          - --load-restrictor
        argument: '''LoadRestrictionsRootOnly'''
        description: if set to 'LoadRestrictionsNone', local kustomizations may load files from outside their root. This does, however, break the relocatability of the kustomization.
      - names:
          - --mount
        argument: '[]'
        description: a list of storage options read from the filesystem
      - names:
          - --network
        argument: "false"
        description: enable network access for functions that declare it
      - names:
          - --network-name
        argument: '''bridge'''
        description: the docker network to run the container in
      - names:
          - -o
          - --output
        argument: ''''''
        description: If specified, write output to this path.
      - names:
          - --reorder
        argument: '''legacy'''
        description: Reorder the resources just before output. Use 'legacy' to apply a legacy reordering (Namespaces first, Webhooks last, etc). Use 'none' to suppress a final reordering.
  - name: label
    description: Update the labels on a resource
    usage: kubectl label [--overwrite] (-f FILENAME | TYPE NAME) KEY_1=VAL_1 ... KEY_N=VAL_N [--resource-version=version] [options]
    options:
      - names:
          - --all
        argument: "false"
        description: Select all resources, in the namespace of the specified resource types
      - names:
          - -A
          - --all-namespaces
        argument: "false"
        description: If true, check the specified action in all namespaces.
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - --field-manager
        argument: '''kubectl-label'''
        description: Name of the manager used to track field ownership.
      - names:
          - --field-selector
        argument: ''''''
        description: Selector (field query) to filter on, supports '=', '==', and '!='.(e.g. --field-selector key1=value1,key2=value2). The server only supports a limited number of field queries per type.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: Filename, directory, or URL to files identifying the resource to update the labels
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process the kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - --list
        argument: "false"
        description: If true, display the labels for a given resource.
      - names:
          - --local
        argument: "false"
        description: If true, label will NOT contact api-server but run locally.
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
      - names:
          - --overwrite
        argument: "false"
        description: If true, allow labels to be overwritten, otherwise reject label updates that overwrite existing labels.
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - --resource-version
        argument: ''''''
        description: If non-empty, the labels update will only succeed if this is the current resource-version for the object. Only valid when specifying a single resource.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
  - name: annotate
    description: Update the annotations on a resource
    usage: kubectl annotate [--overwrite] (-f FILENAME | TYPE NAME) KEY_1=VAL_1 ... KEY_N=VAL_N [--resource-version=version] [options]
    options:
      - names:
          - --all
        argument: "false"
        description: Select all resources, in the namespace of the specified resource types.
      - names:
          - -A
          - --all-namespaces
        argument: "false"
        description: If true, check the specified action in all namespaces.
      - names:
          - --allow-missing-template-keys
        argument: "true"
        description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
      - names:
          - --dry-run
        argument: '''none'''
        description: Must be "none", "server", or "client". If client strategy, only print the object that would be sent, without sending it. If server strategy, submit server-side request without persisting the resource.
      - names:
          - --field-manager
        argument: '''kubectl-annotate'''
        description: Name of the manager used to track field ownership.
      - names:
          - --field-selector
        argument: ''''''
        description: Selector (field query) to filter on, supports '=', '==', and '!='.(e.g. --field-selector key1=value1,key2=value2). The server only supports a limited number of field queries per type.
      - names:
          - -f
          - --filename
        argument: '[]'
        description: Filename, directory, or URL to files identifying the resource to update the annotation
      - names:
          - -k
          - --kustomize
        argument: ''''''
        description: Process the kustomization directory. This flag can't be used together with -f or -R.
      - names:
          - --list
        argument: "false"
        description: If true, display the annotations for a given resource.
      - names:
          - --local
        argument: "false"
        description: If true, annotation will NOT contact api-server but run locally.
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
      - names:
          - --overwrite
        argument: "false"
        description: If true, allow annotations to be overwritten, otherwise reject annotation updates that overwrite existing annotations.
      - names:
          - -R
          - --recursive
        argument: "false"
        description: Process the directory used in -f, --filename recursively. Useful when you want to manage related manifests organized within the same directory.
      - names:
          - --resource-version
        argument: ''''''
        description: If non-empty, the annotation update will only succeed if this is the current resource-version for the object. Only valid when specifying a single resource.
      - names:
          - -l
          - --selector
        argument: ''''''
        description: Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2). Matching objects must satisfy all of the specified label constraints.
      - names:
          - --show-managed-fields
        argument: "false"
        description: If true, keep the managedFields when printing objects in JSON or YAML format.
      - names:
          - --template
        argument: ''''''
        description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
  - name: completion
    description: Output shell completion code for the specified shell (bash, zsh, fish, or powershell)
    usage: kubectl completion SHELL [options]
    options: []
  - name: alpha
    description: Commands for features in alpha
    usage: ""
    options: []
    subcommands:
      - name: events
        description: 'Experimental: List events'
        usage: kubectl alpha events [(-o|--output=)json|yaml|name|go-template|go-template-file|template|templatefile|jsonpath|jsonpath-as-json|jsonpath-file] [--for TYPE/NAME] [--watch] [--event=Normal,Warning] [options]
        options:
          - names:
              - -A
              - --all-namespaces
            argument: "false"
            description: If present, list the requested object(s) across all namespaces. Namespace in current context is ignored even if specified with --namespace.
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --chunk-size
            argument: "500"
            description: Return large lists in chunks rather than all at once. Pass 0 to disable. This flag is beta and may change in the future.
          - names:
              - --for
            argument: ''''''
            description: Filter events to only those pertaining to the specified resource.
          - names:
              - --no-headers
            argument: "false"
            description: When using the default output format, don't print headers.
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
          - names:
              - --types
            argument: '[]'
            description: Output only events of given types.
          - names:
              - -w
              - --watch
            argument: "false"
            description: After listing the requested events, watch for more events.
  - name: api-resources
    description: Print the supported API resources on the server
    usage: kubectl api-resources [flags] [options]
    options:
      - names:
          - --api-group
        argument: ''''''
        description: Limit to resources in the specified API group.
      - names:
          - --cached
        argument: "false"
        description: Use the cached list of resources if available.
      - names:
          - --namespaced
        argument: "true"
        description: If false, non-namespaced resources will be returned, otherwise returning namespaced resources by default.
      - names:
          - --no-headers
        argument: "false"
        description: When using the default or custom-column output format, don't print headers (default print headers).
      - names:
          - -o
          - --output
        argument: ''''''
        description: 'Output format. One of: (wide, name).'
      - names:
          - --sort-by
        argument: ''''''
        description: If non-empty, sort list of resources using specified field. The field can be either 'name' or 'kind'.
      - names:
          - --verbs
        argument: '[]'
        description: Limit to resources that support the specified verbs.
  - name: api-versions
    description: Print the supported API versions on the server, in the form of "group/version"
    usage: kubectl api-versions [options]
    options: []
  - name: config
    description: Modify kubeconfig files
    usage: kubectl config SUBCOMMAND [options]
    options: []
    subcommands:
      - name: current-context
        description: Display the current-context
        usage: kubectl config current-context [flags] [options]
        options: []
      - name: delete-cluster
        description: Delete the specified cluster from the kubeconfig
        usage: kubectl config delete-cluster NAME [options]
        options: []
      - name: delete-context
        description: Delete the specified context from the kubeconfig
        usage: kubectl config delete-context NAME [options]
        options: []
      - name: delete-user
        description: Delete the specified user from the kubeconfig
        usage: kubectl config delete-user NAME [options]
        options: []
      - name: get-clusters
        description: Display clusters defined in the kubeconfig
        usage: kubectl config get-clusters [flags] [options]
        options: []
      - name: get-contexts
        description: Describe one or many contexts
        usage: kubectl config get-contexts [(-o|--output=)name)] [options]
        options:
          - names:
              - --no-headers
            argument: "false"
            description: When using the default or custom-column output format, don't print headers (default print headers).
          - names:
              - -o
              - --output
            argument: ''''''
            description: 'Output format. One of: (name).'
      - name: get-users
        description: Display users defined in the kubeconfig
        usage: kubectl config get-users [flags] [options]
        options: []
      - name: rename-context
        description: Rename a context from the kubeconfig file
        usage: kubectl config rename-context CONTEXT_NAME NEW_NAME [options]
        options: []
      - name: set
        description: Set an individual value in a kubeconfig file
        usage: kubectl config set PROPERTY_NAME PROPERTY_VALUE [options]
        options:
          - names:
              - --set-raw-bytes
            argument: "false"
            description: When writing a []byte PROPERTY_VALUE, write the given string directly without base64 decoding.
      - name: set-cluster
        description: Set a cluster entry in kubeconfig
        usage: kubectl config set-cluster NAME [--server=server] [--certificate-authority=path/to/certificate/authority] [--insecure-skip-tls-verify=true] [--tls-server-name=example.com] [options]
        options:
          - names:
              - --embed-certs
            argument: "false"
            description: embed-certs for the cluster entry in kubeconfig
          - names:
              - --proxy-url
            argument: ''''''
            description: proxy-url for the cluster entry in kubeconfig
      - name: set-context
        description: Set a context entry in kubeconfig
        usage: kubectl config set-context [NAME | --current] [--cluster=cluster_nickname] [--user=user_nickname] [--namespace=namespace] [options]
        options:
          - names:
              - --current
            argument: "false"
            description: Modify the current context
      - name: set-credentials
        description: Set a user entry in kubeconfig
        usage: kubectl config set-credentials NAME [--client-certificate=path/to/certfile] [--client-key=path/to/keyfile] [--token=bearer_token] [--username=basic_user] [--password=basic_password] [--auth-provider=provider_name] [--auth-provider-arg=key=value] [--exec-command=exec_command] [--exec-api-version=exec_api_version] [--exec-arg=arg] [--exec-env=key=value] [options]
        options:
          - names:
              - --client-certificate
              - --client-key
            argument: certfile
            description: 'Bearer token flags:'
          - names:
              - --token
            argument: bearer_token
            description: 'Basic auth flags:'
          - names:
              - --username
              - --password
            argument: basic_user
            description: Bearer token and basic auth are mutually exclusive.
          - names:
              - --auth-provider
            argument: ''''''
            description: Auth provider for the user entry in kubeconfig
          - names:
              - --auth-provider-arg
            argument: '[]'
            description: '''key=value'' arguments for the auth provider'
          - names:
              - --embed-certs
            argument: "false"
            description: Embed client cert/key for the user entry in kubeconfig
          - names:
              - --exec-api-version
            argument: ''''''
            description: API version of the exec credential plugin for the user entry in kubeconfig
          - names:
              - --exec-arg
            argument: '[]'
            description: New arguments for the exec credential plugin command for the user entry in kubeconfig
          - names:
              - --exec-command
            argument: ''''''
            description: Command for the exec credential plugin for the user entry in kubeconfig
          - names:
              - --exec-env
            argument: '[]'
            description: '''key=value'' environment values for the exec credential plugin'
      - name: unset
        description: Unset an individual value in a kubeconfig file
        usage: kubectl config unset PROPERTY_NAME [options]
        options: []
      - name: use-context
        description: Set the current-context in a kubeconfig file
        usage: kubectl config use-context CONTEXT_NAME [options]
        options: []
      - name: view
        description: Display merged kubeconfig settings or a specified kubeconfig file
        usage: kubectl config view [flags] [options]
        options:
          - names:
              - --allow-missing-template-keys
            argument: "true"
            description: If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.
          - names:
              - --flatten
            argument: "false"
            description: Flatten the resulting kubeconfig file into self-contained output (useful for creating portable kubeconfig files)
          - names:
              - --merge
            argument: "true"
            description: Merge the full hierarchy of kubeconfig files
          - names:
              - --minify
            argument: "false"
            description: Remove all information not used by current-context from the output
          - names:
              - -o
              - --output
            argument: '''yaml'''
            description: 'Output format. One of: (json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file).'
          - names:
              - --raw
            argument: "false"
            description: Display raw byte data
          - names:
              - --show-managed-fields
            argument: "false"
            description: If true, keep the managedFields when printing objects in JSON or YAML format.
          - names:
              - --template
            argument: ''''''
            description: Template string or path to template file to use when -o=go-template, -o=go-template-file. The template format is golang templates [http://golang.org/pkg/text/template/#pkg-overview].
  - name: plugin
    description: Provides utilities for interacting with plugins
    usage: kubectl plugin [flags] [options]
    options: []
    subcommands:
      - name: list
        description: List all visible plugin executables on a user's PATH
        usage: kubectl plugin list [flags] [options]
        options:
          - names:
              - --name-only
            argument: "false"
            description: If true, display only the binary name of each plugin, rather than its full path
  - name: version
    description: Print the client and server version information
    usage: kubectl version [flags] [options]
    options:
      - names:
          - --client
        argument: "false"
        description: If true, shows client version only (no server required).
      - names:
          - -o
          - --output
        argument: ''''''
        description: One of 'yaml' or 'json'.
inheritedOptions:
  - names:
      - --add-dir-header
    argument: "false"
    description: 'If true, adds the file directory to the header of the log messages (DEPRECATED: will be removed in a future release, see https://github.com/kubernetes/enhancements/tree/master/keps/sig-instrumentation/2845-deprecate-klog-specific-flags-in-k8s-components)'
  - names:
      - --alsologtostderr
    argument: "false"
    description: 'log to standard error as well as files (no effect when -logtostderr=true) (DEPRECATED: will be removed in a future release, see https://github.com/kubernetes/enhancements/tree/master/keps/sig-instrumentation/2845-deprecate-klog-specific-flags-in-k8s-components)'
  - names:
      - --as
    argument: ''''''
    description: Username to impersonate for the operation. User could be a regular user or a service account in a namespace.
  - names:
      - --as-group
    argument: '[]'
    description: Group to impersonate for the operation, this flag can be repeated to specify multiple groups.
  - names:
      - --as-uid
    argument: ''''''
    description: UID to impersonate for the operation.
  - names:
      - --cache-dir
    argument: '''~/.kube/cache'''
    description: Default cache directory
  - names:
      - --certificate-authority
    argument: ''''''
    description: Path to a cert file for the certificate authority
  - names:
      - --client-certificate
    argument: ''''''
    description: Path to a client certificate file for TLS
  - names:
      - --client-key
    argument: ''''''
    description: Path to a client key file for TLS
  - names:
      - --cluster
    argument: ''''''
    description: The name of the kubeconfig cluster to use
  - names:
      - --context
    argument: ''''''
    description: The name of the kubeconfig context to use
  - names:
      - --insecure-skip-tls-verify
    argument: "false"
    description: If true, the server's certificate will not be checked for validity. This will make your HTTPS connections insecure
  - names:
      - --kubeconfig
    argument: ''''''
    description: Path to the kubeconfig file to use for CLI requests.
  - names:
      - --log-backtrace-at
    argument: :0
    description: 'when logging hits line file:N, emit a stack trace (DEPRECATED: will be removed in a future release, see https://github.com/kubernetes/enhancements/tree/master/keps/sig-instrumentation/2845-deprecate-klog-specific-flags-in-k8s-components)'
  - names:
      - --log-dir
    argument: ''''''
    description: 'If non-empty, write log files in this directory (no effect when -logtostderr=true) (DEPRECATED: will be removed in a future release, see https://github.com/kubernetes/enhancements/tree/master/keps/sig-instrumentation/2845-deprecate-klog-specific-flags-in-k8s-components)'
  - names:
      - --log-file
    argument: ''''''
    description: 'If non-empty, use this log file (no effect when -logtostderr=true) (DEPRECATED: will be removed in a future release, see https://github.com/kubernetes/enhancements/tree/master/keps/sig-instrumentation/2845-deprecate-klog-specific-flags-in-k8s-components)'
  - names:
      - --log-file-max-size
    argument: "1800"
    description: 'Defines the maximum size a log file can grow to (no effect when -logtostderr=true). Unit is megabytes. If the value is 0, the maximum file size is unlimited. (DEPRECATED: will be removed in a future release, see https://github.com/kubernetes/enhancements/tree/master/keps/sig-instrumentation/2845-deprecate-klog-specific-flags-in-k8s-components)'
  - names:
      - --log-flush-frequency
    argument: 5s
    description: Maximum number of seconds between log flushes
  - names:
      - --logtostderr
    argument: "true"
    description: 'log to standard error instead of files (DEPRECATED: will be removed in a future release, see https://github.com/kubernetes/enhancements/tree/master/keps/sig-instrumentation/2845-deprecate-klog-specific-flags-in-k8s-components)'
  - names:
      - --match-server-version
    argument: "false"
    description: Require server version to match client version
  - names:
      - -n
      - --namespace
    argument: ''''''
    description: If present, the namespace scope for this CLI request
  - names:
      - --one-output
    argument: "false"
    description: 'If true, only write logs to their native severity level (vs also writing to each lower severity level; no effect when -logtostderr=true) (DEPRECATED: will be removed in a future release, see https://github.com/kubernetes/enhancements/tree/master/keps/sig-instrumentation/2845-deprecate-klog-specific-flags-in-k8s-components)'
  - names:
      - --password
    argument: ''''''
    description: Password for basic authentication to the API server
  - names:
      - --profile
    argument: '''none'''
    description: Name of profile to capture. One of (none|cpu|heap|goroutine|threadcreate|block|mutex)
  - names:
      - --profile-output
    argument: '''profile.pprof'''
    description: Name of the file to write the profile to
  - names:
      - --request-timeout
    argument: '''0'''
    description: The length of time to wait before giving up on a single server request. Non-zero values should contain a corresponding time unit (e.g. 1s, 2m, 3h). A value of zero means don't timeout requests.
  - names:
      - -s
      - --server
    argument: ''''''
    description: The address and port of the Kubernetes API server
  - names:
      - --skip-headers
    argument: "false"
    description: 'If true, avoid header prefixes in the log messages (DEPRECATED: will be removed in a future release, see https://github.com/kubernetes/enhancements/tree/master/keps/sig-instrumentation/2845-deprecate-klog-specific-flags-in-k8s-components)'
  - names:
      - --skip-log-headers
    argument: "false"
    description: 'If true, avoid headers when opening log files (no effect when -logtostderr=true) (DEPRECATED: will be removed in a future release, see https://github.com/kubernetes/enhancements/tree/master/keps/sig-instrumentation/2845-deprecate-klog-specific-flags-in-k8s-components)'
  - names:
      - --stderrthreshold
    argument: "2"
    description: 'logs at or above this threshold go to stderr when writing to files and stderr (no effect when -logtostderr=true or -alsologtostderr=false) (DEPRECATED: will be removed in a future release, see https://github.com/kubernetes/enhancements/tree/master/keps/sig-instrumentation/2845-deprecate-klog-specific-flags-in-k8s-components)'
  - names:
      - --tls-server-name
    argument: ''''''
    description: Server name to use for server certificate validation. If it is not provided, the hostname used to contact the server is used
  - names:
      - --token
    argument: ''''''
    description: Bearer token for authentication to the API server
  - names:
      - --user
    argument: ''''''
    description: The name of the kubeconfig user to use
  - names:
      - --username
    argument: ''''''
    description: Username for basic authentication to the API server
  - names:
      - -v
      - --v
    argument: "0"
    description: number for the log level verbosity
  - names:
      - --vmodule
    argument: ""
    description: comma-separated list of pattern=N settings for file-filtered logging
  - names:
      - --warnings-as-errors
    argument: "false"
    description: Treat warnings received from the server as errors and exit with a non-zero exit code
version: 'Client Version: version.Info{Major:"1", Minor:"25", GitVersion:"v1.25.2", GitCommit:"5835544ca568b757a8ecae5c153f317e5736700e", GitTreeState:"clean", BuildDate:"2022-09-21T14:33:49Z", GoVersion:"go1.19.1", Compiler:"gc", Platform:"linux/amd64"}'
tldr: |
  > Command-line interface for running commands against Kubernetes clusters.
  > Some subcommands such as `kubectl run` have their own usage documentation.
  > More information: <https://kubernetes.io/docs/reference/kubectl/>.

  - List information about a resource with more details:

  `kubectl get {{pod|service|deployment|ingress|...}} -o wide`

  - Update specified pod with the label 'unhealthy' and the value 'true':

  `kubectl label pods {{name}} unhealthy=true`

  - List all resources with different types:

  `kubectl get all`

  - Display resource (CPU/Memory/Storage) usage of nodes or pods:

  `kubectl top {{pod|node}}`

  - Print the address of the master and cluster services:

  `kubectl cluster-info`

  - Display an explanation of a specific field:

  `kubectl explain {{pods.spec.containers}}`

  - Print the logs for a container in a pod or specified resource:

  `kubectl logs {{pod_name}}`

  - Run command in an existing pod:

  `kubectl exec {{pod_name}} -- {{ls /}}`
