name: trash
description: trash
usage: trash [OPTION]... FILE...
options:
  - names:
      - --version
    argument: ""
    description: show program's version number and exit
  - names:
      - -h
      - --help
    argument: ""
    description: show this help message and exit
  - names:
      - -d
      - --directory
    argument: ""
    description: ignored (for GNU rm compatibility)
  - names:
      - -f
      - --force
    argument: ""
    description: ignored (for GNU rm compatibility)
  - names:
      - -i
      - --interactive
    argument: ""
    description: ignored (for GNU rm compatibility)
  - names:
      - -r
      - -R
      - --recursive
    argument: ""
    description: ignored (for GNU rm compatibility)
  - names:
      - -v
      - --verbose
    argument: ""
    description: explain what is being done
version: trash 0.17.1.14
tldr: |
  > A CLI for managing the trashcan / recycling bin.
  > More information: <https://github.com/andreafrancia/trash-cli>.

  - Delete a file and send it to the trash:

  `trash {{path/to/file}}`

  - List all files in the trash:

  `trash-list`

  - Interactively restore a file from the trash:

  `trash-restore`

  - Empty the trash:

  `trash-empty`

  - Permanently delete all files in the trash which are older than 10 days:

  `trash-empty {{10}}`

  - Remove all files in the trash, which match a specific blob pattern:

  `trash-rm "{{*.o}}"`

  - Remove all files with a specific original location:

  `trash-rm {{/path/to/file_or_directory}}`
