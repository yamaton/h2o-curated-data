#compdef _code code

# Generated with h2o 0.1.18


function _code {
    local line state

    function _commands {
        local -a commands
        commands=(
        )
        _describe 'command' commands
    }
 

    _arguments -C \
        '(-d --diff)'{-d,--diff}'[Compare two files with each other.]:file:_files' \
        '(-a --add)'{-a,--add}'[Add folder(s) to the last active window.]' \
        '(-g --goto)'{-g,--goto}'[Open a file at the path on the specified line and character position.]:file:_files' \
        '(-n --new-window)'{-n,--new-window}'[Force to open a new window.]' \
        '(-r --reuse-window)'{-r,--reuse-window}'[Force to open a file or folder in an already opened window.]' \
        '(-w --wait)'{-w,--wait}'[Wait for the files to be closed before returning.]' \
        '--locale[The locale to use (e.g. en-US or zh-TW).]' \
        '(-h --help)'{-h,--help}'[Print usage.]' \
        '--list-extensions[List the installed extensions.]' \
        '--show-versions[Show versions of installed extensions, when using --list-extensions.]' \
        '--category[Filters installed extensions by provided category, when using --list-extensions.]' \
        '--install-extension[Installs or updates the extension. The identifier of an extension is always `${publisher}.${name}`. Use `--force` argument to update to latest version. To install a specific version provide `@${version}`. For example: '\''vscode.csharp@1.2.3'\''.]:file:_files' \
        '--uninstall-extension[Uninstalls an extension.]' \
        '--enable-proposed-api[Enables proposed API features for extensions. Can receive one or more extension IDs to enable individually.]' \
        '(-v --version)'{-v,--version}'[Print version.]' \
        '--verbose[Print verbose output (implies --wait).]' \
        '--log[Log level to use. Default is '\''info'\''. Allowed values are '\''critical'\'', '\''error'\'', '\''warn'\'', '\''info'\'', '\''debug'\'', '\''trace'\'', '\''off'\''.]' \
        '(-s --status)'{-s,--status}'[Print process usage and diagnostics information.]' \
        '--prof-startup[Run CPU profiler during startup.]' \
        '--disable-extensions[Disable all installed extensions.]' \
        '--disable-extension[Disable an extension.]' \
        '--sync[Turn sync on or off.]' \
        '--inspect-extensions[Allow debugging and profiling of extensions. Check the developer tools for the connection URI.]' \
        '--inspect-brk-extensions[Allow debugging and profiling of extensions with the extension host being paused after start. Check the developer tools for the connection URI.]' \
        '--disable-gpu[Disable GPU hardware acceleration.]' \
        '--max-memory[Max memory size for a window (in Mbytes).]' \
        '1: :_commands' \
        '*: :_files'

    case $state in
    (cmd)
        _commands
        ;;
    (subcmd)
        case $line[1] in
        esac
        ;;
     esac

}

