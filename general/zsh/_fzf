#compdef _fzf fzf

# Auto-generated with h2o


function _fzf {
    local line state

    function _commands {
        local -a commands
        commands=(
        )
        _describe 'command' commands
    }
 

    _arguments -C \
        {-x,--extended}'[(enabled by default; +x or --no-extended to disable)]' \
        {-e,--exact}'[Enable Exact-match]' \
        '--algo[Fuzzy matching algorithm: \[v1|v2\] (default: v2)]' \
        '-i[Case-insensitive match (default: smart-case match)]' \
        '--literal[Do not normalize latin script letters before matching]' \
        {-n,--nth}'[Comma-separated list of field index expressions for limiting search scope. Each can be a non-zero integer or a range expression (\[BEGIN\]..\[END\]).]' \
        '--with-nth[Transform the presentation of each line using field index expressions]' \
        {-d,--delimiter}'[Field delimiter regex (default: AWK-style)]' \
        '--tac[Reverse the order of the input]' \
        '--disabled[Do not perform search]' \
        '--tiebreak[Comma-separated list of sort criteria to apply when the scores are tied \[length|begin|end|index\] (default: length)]' \
        {-m,--multi}'[Enable multi-select with tab/shift-tab]' \
        '--no-mouse[Disable mouse]' \
        '--bind[Custom key bindings. Refer to the man page.]' \
        '--cycle[Enable cyclic scroll]' \
        '--keep-right[Keep the right end of the line visible on overflow]' \
        '--scroll-off[Number of screen lines to keep above or below when scrolling to the top or to the bottom (default: 0)]' \
        '--no-hscroll[Disable horizontal scroll]' \
        '--hscroll-off[Number of screen columns to keep to the right of the highlighted substring (default: 10)]' \
        '--filepath-word[Make word-wise movements respect path separators]' \
        '--jump-labels[Label characters for jump and jump-accept]' \
        '--height[Display fzf window below the cursor with the given height instead of using fullscreen]' \
        '--min-height[Minimum height when --height is given in percent (default: 10)]' \
        '--layout[Choose layout: \[default|reverse|reverse-list\]]' \
        '--border[Draw border around the finder \[rounded|sharp|horizontal|vertical|]' \
        '--margin[Screen margin (TRBL | TB,RL | T,RL,B | T,R,B,L)]' \
        '--padding[Padding inside border (TRBL | TB,RL | T,RL,B | T,R,B,L)]' \
        '--info[Finder info style \[default|inline|hidden\]]' \
        '--prompt[Input prompt (default: '\''> '\'')]' \
        '--pointer[Pointer to the current line (default: '\''>'\'')]' \
        '--marker[Multi-select marker (default: '\''>'\'')]' \
        '--header[String to print as header]' \
        '--header-lines[The first N lines of the input are treated as header]' \
        '--header-first[Print header before the prompt line]' \
        '--ellipsis[Ellipsis to show when line is truncated (default: '\''..'\'')]' \
        '--ansi[Enable processing of ANSI color codes]' \
        '--tabstop[Number of spaces for a tab character (default: 8)]' \
        '--color[Base scheme (dark|light|16|bw) and/or custom colors]' \
        '--no-bold[Do not use bold text]' \
        '--history[History file]':file:_files \
        '--history-size[Maximum number of history entries (default: 1000)]' \
        '--preview[Command to preview highlighted line ({})]' \
        '--preview-window[Preview window layout (default: right:50%) \[up|down|left|right\]\[,SIZE\[%\]\] \[,\[no\]wrap\]\[,\[no\]cycle\]\[,\[no\]follow\]\[,\[no\]hidden\] \[,border-BORDER_OPT\] \[,+SCROLL\[OFFSETS\]\[/DENOM\]\]\[,~HEADER_LINES\] \[,default\]]' \
        {-q,--query}'[Start the finder with the given query]' \
        {-1,--select-1}'[Automatically select the only match]' \
        {-0,--exit-0}'[Exit immediately when there'\''s no match]' \
        {-f,--filter}'[Filter mode. Do not start interactive finder.]' \
        '--print-query[Print query as the first line]' \
        '--expect[Comma-separated list of keys to complete fzf]' \
        '--read0[Read input delimited by ASCII NUL characters]' \
        '--print0[Print output delimited by ASCII NUL characters]' \
        '--sync[Synchronous search for multi-staged filtering]' \
        '--version[Display version information and exit]' \
        '*: :_files'

    case $state in
    (cmd)
        _commands
        ;;
    (subcmd)
        case $line[1] in
        esac
        ;;
     esac

}

