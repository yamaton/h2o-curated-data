#compdef _gst-stats-1.0 gst-stats-1.0

# Auto-generated with h2o


function _gst-stats-1.0 {
    local line state

    function _commands {
        local -a commands
        commands=(
        )
        _describe 'command' commands
    }
 

    _arguments -C \
        {-h,--help}'[Show help options]' \
        '--help-all[Show all help options]' \
        '--help-gst[Show GStreamer Options]' \
        '--version[Print version information and exit]' \
        '*: :_files'

    case $state in
    (cmd)
        _commands
        ;;
    (subcmd)
        case $line[1] in
        esac
        ;;
     esac

}

