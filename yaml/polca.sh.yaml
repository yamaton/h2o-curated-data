name: polca.sh
description: polca.sh
options:
  - names:
      - -a
    argument: <assembly contigs or scaffolds>
    description: Assembly contigs or scaffolds
  - names:
      - -r
    argument: <Illumina_reads_fastq1 Illumina_reads_fastq>
    description: Illumina reads fastq
  - names:
      - -m
    argument: '[memory per thread to use in samtools sort]'
    description: Memory per thread to use in samtools sort
  - names:
      - -t
    argument: INT
    description: Number of threads
